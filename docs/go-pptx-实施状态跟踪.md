# go-pptx 实施状态跟踪

> 按《go-pptx 项目实施计划》§12 执行顺序滚动更新；工作包完成标准见计划 §5.2。
> 更新时机：每次 PR 合并 / 里程碑评审后由负责人更新本表。

## 当前阶段：M8 创新扩展 II（**CLONE-02 跨文档受限复制已落地；QA-01 fuzz 八目标落地（打开 + 文本编辑 + 模板绑定）；AT-14 恶意包 panic 修复；M0 垂直验证测试落地；TIMIR-01 边缘 case 修复已完成；DIFF-01 + SHAPE-CREATE + STALE-GUARD 已完成**；M7 四工作包全数落地；M6 七工作包已收尾；M2–M5 代码项已收口；闭环一/AT 真实语料验收待样本与客户端冒烟）

### 最近更新

| 日期 | 进展 | 备注 |
|---|---|---|
| 2026-09-10 | **CLONE-02 跨文档受限页面复制（M8 收尾项）**：`Presentation.CopySlideFrom(src *Slide, *ClonePolicy) (*Slide, error)`——源页全部内容复制为追加到当前文档 `sldIdLst` 末尾的新页面。① **clonePlan 引入 srcDoc/crossDoc 分离**：跨文档时 srcDoc 持有读视图，`patchedCloneRels/cloneWalkContainer/buildClonePlan/applyClonePlan` 全部参数化走 srcDoc（applyClonePlan 用 `plan.srcDoc.partBytes(...)` 而非 `p.partBytes(...)` 避免跨包读到源 Part 不存在的错误）。② **matchReusePart 内容字节匹配复用**：跨文档 + reuse 类型时对目标文档全部候选 slideLayout（沿 slideMaster 关系流展开）/ notesMaster 按字节等价比对；命中复用，未命中整体拒绝 `ErrUnsupportedEdit`。③ **媒体强制独立**：跨文档时不论 policy.IndependentMedia，media 关系都走"分配新 Part 名 + 复制字节"路径。④ **src.p == dst dispatch 回同文档 Clone**：`CopySlideFrom` 显式判 `src.p == dst` → `src.Clone(policy)`，避免语义分裂。⑤ **CLONE-01 同架构兼容**：复用 `cloneWalkContainer`（visited 防循环）+ `classifyCloneRel`（reuse/backref/media/notes/chart/workbook/unknown）+ `allocCloneName` + `applyClonePlan`（两阶段提交 + 失败恢复暂存区）；CLONE-01 现有 10 项测试零回归。⑥ **受限范围拒绝整体生效**：未知内部关系类型（OLE/SmartArt 等）/ notesSlide 回引非源页 / 嵌入工作簿自带关系流 → `cloneWalkContainer` 整体 ErrUnsupportedEdit 退出（AT-13 跨包口径），applyClonePlan 失败恢复暂存区保证零残留。⑦ **测试 4 项**（clone_test.go 末尾）：`TestCopySlideFrom_BasicLayoutReuse`（版式字节一致复用）/ `TestCopySlideFrom_ReuseLayoutMismatch`（篡改 slideLayout1 后整体拒绝+零残留）/ `TestCopySlideFrom_SameDocDispatchedToClone`（src.p==dst 走 Clone）/ `TestCopySlideFrom_SaveRoundTrip`（复制→Write→OpenReader 重开→slideLayout rel 复用命中）。⑧ **capability 回填**：`clone.cross_document` Partial（Notes 写明跨包语义差异 + 版式/母版匹配复用策略，Limits 登记拒绝清单）；`README.md` 增 CLONE-02 条目；`doc.go` 与 `docs/M7-排序输入.md` 同步"CLONE-02 已于 M8 收尾"。⑨ **质量门**：gofmt/vet/全量测试（CLONE-01 10 项 + CLONE-02 4 项）/ cmd/pptx / audioprobe / opc / videoprobe / xmlstore / ir / wasm/check 八包零失败 + CGO=0 Linux + js/wasm + wasip1/wasm 三交叉构建全过；根包覆盖率 77.1% 持平 | clone.go（srcDoc/crossDoc 分离 + matchReusePart + CopySlideFrom API，+177/-32 行）+ clone_test.go（4 用例 + 3 helper readAllParts/writeAllParts/dumpPending，+193 行）；capability.go 增 clone.cross_document 段；README.md / doc.go / M7-排序输入.md 同步 |
| 2026-09-10 | **QA-01 fuzz 补模板绑定路径（八目标）**：在打开链路（五目标）+ 文本编辑（两目标）之上补 `FuzzBind`——对 `Presentation.Bind` 喂随机结构化数据源。① **确定性解码器 `fuzzBindDecode`**：Bind 输入是 `map[string]any` 非字符串，JSON 解码会把数字归一到 float64、无法触及 `formatBindValue` 的 int*/uint*/bool/string 强类型分支，故用 tag 字节显式编码类型（nil/string/int64/bool/float64/[]any/[]map[string]any/map[string]any），每个输入都产生有效数据（不浪费在解码失败上），并覆盖嵌套 map、强类型切片（走 asSlice 的 reflect 分支）、nil 边界。② **综合模板底座 `bindFuzzDeck`**：单页同时含内联占位符 `{{title}}`、条件段落 `{{#if show}}`、表格行循环 `{{#each rows}}`（字段 `{{name}}`/`{{qty}}`），使随机数据同时穿过值呈现 / 条件真值 / 切片展开三类绑定路径。③ 非严格模式（`WithBindStrict(false)`）让缺键/类型不符走 Warning 诊断路径而非短路。断言不 panic + 成功后保存重开成功。短时 fuzz 冒烟 20s 约 13.1 万次执行、236 个 interesting 输入零崩溃 | fuzz_bind_test.go（FuzzBind + bindDecoder/fuzzBindDecode/bindFuzzDeck）。根包覆盖率 77.1% 持平（fuzz 种子不增语句覆盖）；gofmt/vet/全量测试 + CGO=0 Linux + js/wasm + wasip1/wasm 三交叉构建通过 |
| 2026-09-10 | **QA-01 fuzz 补文本编辑路径（七目标）**：在打开链路 fuzz（五目标）之上补上编辑路径随机输入 fuzz。① **FuzzReplaceText**：以合法正文种子（Hello World）为底座，对 `TextFrame.ReplaceText(old, replacement)` 喂随机字符串——覆盖跨 Run 匹配、空 old（无匹配）、空 replacement（删除）、换行/实体等特殊字符。② **FuzzSetPlainText**：对 `TextFrame.SetPlainText(text)`（'\n' 分行结构化替换）喂任意换行/空行/超长单行/实体字符。③ 共用辅助 `roundTripReopen`（Write → OpenReader 重开 → Slides() 深入解析），把"保真补丁不产生损坏包"落为可回归断言。短时 fuzz 冒烟累计约 8.3 万次执行零崩溃 | fuzz_edit_test.go（2 个 Fuzz 目标 + roundTripReopen 辅助）。根包覆盖率 77.0%→**77.1%**；gofmt/vet/全量测试 + CGO=0 Linux + js/wasm + wasip1/wasm 三交叉构建通过 |
| 2026-09-10 | **AT-14 恶意包 panic 修复 + QA-01 fuzz 五目标落地**：① **缺陷发现与修复**：`OpenReader`/`Open`/`New` 三入口曾通过 `mustMainPart(pk)` 在"opc.Load 成功但主 Part 发现失败"时 **panic**——但 Load 只保证根关系流存在、不保证 officeDocument 关系存在/为内部目标/目标 Part 落盘，恶意包可在 Load 通过后触发 MainPart 失败 → panic（违反 AT-14"恶意包预算内失败、无 panic"）。修复：三入口改为显式 `pk.MainPart()` 错误返回（复用 mapOCError 映射；`Open` 失败时正确 `f.Close()`），删除 `mustMainPart` 死代码。② **回归测试** `openreader_test.go`（2 用例）：`TestOpenReader_MissingOfficeDocument`（根关系流无 officeDocument → ErrMalformedPackage 非 panic；修复前确认 panic 于 presentation.go:642）、`TestOpenReader_ExternalOfficeDocument`（officeDocument 为外部目标 → ErrMalformedPackage）。③ **QA-01 fuzz 交付物**（5 个 Fuzz 目标，全部无 recover——panic 即崩溃证据）：`internal/opc/fuzz_test.go` 的 `FuzzLoad`（完整 Load：Content Types + 关系流）+ `FuzzScan`（索引第一道防线：条目名/预算/重复条目）；`internal/xmlstore/fuzz_test.go` 的 `FuzzScanner`（词法层：未闭合/错配/坏实体/非法码点/超长 DOCTYPE）+ `FuzzIndex`（索引树：深度预算、命名空间解析）；根包 `fuzz_test.go` 的 `FuzzOpenReader`（打开链路 + 成功后触发 Slides() 深入 presentation.xml 解析）。种子含最小合法 OPC 包/合法模板包/无 officeDocument 包/空输入/截断 ZIP 头。短时 fuzz 冒烟累计约 350 万次执行零崩溃。④ **断言策略**：错误分类用 `errors.Is` 严格限定（Load/Scan→ErrMalformedPackage∪ErrLimitExceeded；Scanner→ErrEncoding；Index→ErrEncoding∪ErrMalformed∪ErrDepthLimit），任何意外错误类型即 fuzz 发现；成功路径校验非 nil + 节点遍历不越界 | presentation.go（三入口显式 MainPart 错误处理、删 mustMainPart）+ openreader_test.go（2 用例）+ fuzz_test.go ×3（5 个 Fuzz 目标）。根包覆盖率 77.0% 持平；gofmt/vet/全量测试 + CGO=0 Linux + js/wasm + wasip1/wasm 三交叉构建通过 |
| 2026-09-10 | **M0 垂直验证测试落地（实施计划 §123 退出标准，合成语料）**：新增 `vertical_test.go`（3 用例），把计划里"对含动画与未知扩展的页面仅修改一个普通文本 Run → 保存 → 逐 Part 哈希比对（B1）+ 同节点未知区字节不变断言"落成可回归的自动化断言。① **与既有 B1 测试的分工**：`internal/opc/saveplan_test.go` 的 `TestSavePlanUnchangedIsB1` 验证**空变更集**（纯 OPC 层，不经过公共 API 编辑路径）；本文件验证**非空变更集**——确实改了一个 Run，此时要求 ① 其它条目字节一致 ② 被改 Part 内部未触碰区域（p:timing 动画子树 + p:extLst 未知扩展）字节不变。后者是"保真补丁"相对"重新序列化整棵树"的核心优势，也是 ANIM-02 / TIMIR-01 / OPC-02 三方交叠处最易回归处。② **合成 fixture**：`verticalSlideXML()` 组装含正文占位符（英中两段，英文段含替换目标 `REPORT_BODY`）+ `p:extLst` 未知扩展（完全不认识的 `vendor:` 命名空间、含实体与嵌套）+ 真实形态 PowerPoint 点击淡入动画树（tmRoot → par → cTn → clickEffect → set + animEffect，含自闭合 cond——正是 TIMIR-01 之前会解析错位的形态）；CT_Slide 子元素次序遵循 ECMA（cSld → clrMapOvr → timing）。复用 `minimalTemplateParts()` + `buildPackageZip()` 直接组包，无需 `New()` + zip 手术。③ **三用例**：`TestVertical_B1SingleRunEdit`（逐条目 SHA-256 比对，只允许 `ppt/slides/slide1.xml` 变化；直接读 `archive/zip` 容器条目而非经 opc 层，校验"实际落盘字节"）、`TestVertical_UnknownExtAndTimingBytePreserved`（同一 Part 内两个关键区域作为**字面子串**断言保留 + 断言编辑确实生效 + 断言第二段落未受影响）、`TestVertical_TimingIRSurvivesRoundTrip`（编辑→保存→重开后 `TimingTreeRaw()` 字节不退化）。④ **故障注入验证（关键）**：临时在编辑路径中追加一次 `SetCoreProperties`（改另一个 Part），B1 用例如期失败并报 `changed entries = [docProps/core.xml ppt/slides/slide1.xml]`——证明断言非空跑；随后还原。⑤ **语料缺口仍在**：testdata/corpus/ 无真实样本（QA-01 长期阻塞项），本文件用合成语料覆盖断言结构；真实语料回填后保留断言骨架、只换 fixture 来源 | vertical_test.go（3 用例 + `verticalDeck`/`zipEntryHashes`/`diffHashes` 三个辅助）。根包覆盖率 76.9%→**77.0%**；gofmt/vet/全量测试 + CGO=0 Linux + js/wasm + wasip1/wasm 三交叉构建通过 |
| 2026-09-10 | **TIMIR-01 边缘 case 修复（xmlstore 重写解析器）**：原 std encoding/xml.Decoder 在 cTn 同时含 stCondLst（cond 自闭合）与紧随空 childTnLst 的组合上触发 depth 同步偏差——返回诊断而非崩溃。① **重写 `ir/timingir.go` 解析层**：以 `internal/xmlstore.Index`（底层 `xmlstore.Scanner`）构建节点索引树，自闭合元素仅发射 `TokenStart{ SelfClosing: true }`，不再产生独立 End 事件，闭合校验由 Scanner 元素栈负责——彻底消除 std 自闭合 depth 同步问题。保留全部 `tmlCTn` AST 与所有投影函数（`projectCTn`/`projectMedia`/`projectAnim`/`projectTgt`/`projectCond`/`classifyAnim`/`eventFromString`/`durationsFromProto`/`delaysFromProto`/`opaqueFromQName`）；公开 API（`projectTimingTree`/TimingNode/Condition/Target/AnimationEffect/PageTiming/TimingTotals/OpaqueNode/TimingNodeKind/EffectClass/TimeEventKind/Estimate/TriggerRef）签名不变。② **诊断信息升级**：解析错误现在携带 xmlstore 字节偏移（`*xmlstore.SyntaxError`/`*xmlstore.DepthError`），便于 §24 验收"诊断可定位到字节偏移"。新增 `TIMIR_BAD_ROOT`（timing 根元素不是 `<timing>`）/ `TIMIR_NON_PAR_IN_TNLST`（tnLst 下出现非 par 子元素）/ `TIMIR_TIMING_NON_TNLST`（timing 下出现非 tnLst 直接子元素）三种诊断。③ **回归测试 3 项**（timingir_test.go）：`TestTimingIR_SelfClosingCondEmptyChildTnLst`（cTn 同时含自闭合 cond + 空 childTnLst——之前 std xml 报错，现正确解析且无 TIMIR_PARSE_ERR）、`TestTimingIR_SelfClosingCondFollowedBySubtree`（自闭合 cond 后紧跟带动画的 childTnLst，验证子树完整保留且 anim.spTgt.spid=42 可达）、`TestTimingIR_DepthLimitReportsError`（50 层嵌套 childTnLst 在默认深度预算 256 下正常解析）。原有 13 项测试全部零回归。④ **capability 状态升级**：`animation.timing_ir` Partial → **Supported**（Limits 仅保留"未识别子元素落记为 OpaqueNode"这一语义级承诺；自闭合 + 空 childTnLst 边缘 case 已彻底消除）；`capability_test.go` 的 `TestCapability_FutureWorkMarkedUntested` 同步把 TIMIR-01 从 Partial 断言改为 Supported | ir/timingir.go（重写解析层，去除 std encoding/xml 包 import，引入 internal/xmlstore；包注释"已知限制"段落替换为实现说明）+ ir/timingir_test.go（新增 3 项回归测试）。覆盖率 ir 71.5%→**71.6%**；gofmt/vet/全量测试 + CGO=0 Linux + js/wasm + wasip1/wasm 三交叉构建通过 |
| 2026-09-10 | **DIFF-01 语义 diff 与审计（M8 首项）**：`ir.Diff(a, b, opts...)` 以 IR 为输入视图比较两份文档，返回 `DiffReport`（schemaVersion="go-pptx.diff/1.0" 独立管理）。① **NodePath 溯源**：`Shape` 接口新增 `NodePath() string`（shapeNode 统一实现，渲染句柄路径为 `p:sld/p:cSld/p:spTree/p:sp[2]` 形态；句柄失效返回空串不报错，避免诊断路径成为报告生成失败源），`ir.Shape` 透出 `NodePath` 字段——§24 验收"opaque 定位可回溯到 Part/NodePath"的底座。② **页面对齐（关键设计）**：初版按内容签名 LCS 会把"改了一个字"误判为删页+加页——改为**加权 LCS**：SlideID 相同为强匹配（1.0）、SlideID 不同且都非零为不同页（0）、其余按形状 ID 集合 Jaccard 相似度（≥0.5 配对为同一页两次修订）；顺序 LCS 无法表达的乱序残留（如两页互换）以 ≥0.8 高阈值二次配对并标 DiffPageMoved；超 100 万格退化为线性对齐（同 SlideID 优先+下标）防超大文档失控。③ **页内比较**：形状按 ShapeID 配对（含组递归），逐字段比较文本（WithIgnoreWhitespace 归一化空白）、几何 Box（WithIgnoreGeometry 跳过）、类型、名称、替代文本、装饰标记、表格行列数、图表类型；备注（WithIgnoreNotes）与动画时序摘要（TIMIR-01：节点数/估计数/媒体/opaque 计数）单独成项；core 属性（标题/作者/创建时间）单独成项。④ **opaque diff**：两侧 IR 投影差异聚合为 OpaqueRegion（Part+NodePath+Reason），不猜测内部变化；MaxEntries 截断保护（EntriesTruncated 标记，默认 10000）。⑤ **CLI `pptx diff`**：只读，`--output/--stdout/--overwrite`、三个 `--ignore-*`、`--max-entries`；两侧用同一份 ir.DefaultOptions 导出确保投影维度一致 | ir/diff.go（~650 行）+ ir/diff_test.go（16 用例：文本改动不误判删页/相同文档零条目/页面增删移动/两页互换标移动/乱序残留二次配对/形状增删/几何文本名称变更/表格尺寸/图表类型/时序摘要变化/备注变化/忽略选项三件套/opaque 区域/MaxEntries 截断/组形状递归/nil 容错）+ shapes.go（Shape.NodePath）+ ir/ir.go（Shape.NodePath 透出）+ cmd/pptx/diff.go + cmd/pptx/diff_test.go（4 用例：文本改动端到端+统计断言、相同文档零条目、缺参数 usage、--output 与重复写入 ExitResource）+ capability 回填 diff.semantic_audit Untested→**Partial**（Limits 三条：对齐启发式、opaque 聚合不深入元素级、图表/媒体仅文件级摘要）。覆盖率 ir 71.5%、cmd/pptx 65.5%、根包 76.8%；gofmt/vet/全量测试 + CGO=0 Linux + js/wasm + wasip1/wasm 三交叉构建通过 |
| 2026-09-10 | **TPL-01 模板数据绑定引擎（M7 第四项，收尾）**：ADR 013——绑定构建在保真补丁与跨 Run 替换之上，不引入第二套编辑路径。① **重构 replace.go**：抽出 `replacePatches(doc, para, old, replacement, opts)`（原 `Paragraph.ReplaceText` 的补丁构造部分），使 TEXT-02 单段落事务与 TPL-01 按 Part 聚合两种粒度共用同一条保真替换路径。② **`bind.go`（~800 行）+ `bind_test.go`（20 用例）**：`Presentation.Bind(data, opts...)` 两阶段——plan 纯读取（扫描全页形状/表格/图表、解析数据、预检图表可写性，任何语义错误在此返回、不落补丁）+ apply 按 Part 聚合、单事务 commit（**无部分写入**）。③ **语法**：内联占位符 `{{path}}`（点分路径，支持嵌套 map、切片下标、结构体字段；跨 Run 匹配，占位符被拆到多个 Run 也能命中）、条件段落 `{{#if path}}…{{/if}}`（标记独占段落，栈式嵌套；假则删除标记与块内段落；**全删保护**——文本体将被清空时保留一个空段落，避免产出无段落 txBody）、表格行循环 `{{#each path}}…{{/each}}`（标记须同行配对、位于单元格内；模板行按数据条数复制，条目为作用域，`{{.}}` 为标量条目；空数据删除模板行，表格将空时 ErrUnsupportedEdit 拒绝）、图表数据绑定（数据源中值为 `ChartData` 且键=图表形状名 → `chartBindPreflight` 复刻 SetData 前置校验后走 CHART-01 SetData）。④ **关键实现决策**：同段落多个占位符的 rebuild 补丁会重叠（都覆盖整段 Run），故 apply 与行渲染统一采用「按起始偏移**降序**回放 + 同一段落逐个重新索引（`resolvePath`）」；行循环渲染用临时包装文档（声明 DrawingML 为默认命名空间，兼容前缀与默认两种写法）索引模板行字节后再剥离包装。⑤ **`pptx bind --data data.json --output out.pptx [--overwrite] [--loose]`** CLI 子命令：`cmd/pptx/bind.go` + `bind_test.go`（4 用例：正常渲染并回读输出、缺键不写输出且 --loose 可通过、缺标志位 usage、非法 JSON usage）——图表绑定无法由 JSON 表达，注释中明确需走 SDK。⑥ **capability 回填**：`template.binding` Untested → **Partial**（Limits 登记不覆盖备注页/母版、行循环内不支持嵌套条件、图表绑定需 SDK 传 ChartData） | bind.go + bind_test.go（20 用例：内联替换、跨 Run 占位符、嵌套与下标路径、严格缺键失败与无部分写入、宽松模式保留原文+诊断、不支持值类型、条件真假与全删保护、未闭合 #if 拒绝、行循环 3 条扩行/空数据删行/单行表格拒绝/标记不配对/非切片拒绝/#each 出现在非表格拒绝、图表绑定、原子性字节不变与 revision 不变、Parts 与 revision、Closed 守卫）+ replace.go 重构（TEXT-02 回归全绿）+ cmd/pptx/bind.go、cmd/pptx/bind_test.go、cmd/pptx/main.go 注册。覆盖率根包 77.2%→**76.9%**（新增分支多）、cmd/pptx 61.2%→**63.6%**；gofmt/vet/全量测试 + CGO=0 Linux + js/wasm + wasip1/wasm 三交叉构建通过 |
| 2026-09-10 | **STALE-GUARD 句柄失效语义修复（SHAPE-CREATE 暴露的项目级 bug）**：原 `shapeNode.locate` 仅靠 `[ns,local,序号]` 路径下降，兄弟元素被增删/移动后 path 仍能解析到**相邻兄弟**（locate 不报错、ID/Name 错配到错误元素）。修复：① **`shapeNode` 新增 `idHint ShapeID` 字段**——构造时通过 `shapeCNvPrID`/`shapeNodeIDFromIDStr`/`shapeNodeIDFromRecord` 三个 helper 记录 cNvPr@id；create.go/audio.go/audiotiming.go/chart.go/picture.go/video.go/classifyShape（shapes.go）共 9 处构造点全部补全 idHint。② **`shapeNode.locate` 改为 idHint 主身份 + path 父容器提示**：先按 `path[:-1]` 解析出 spTree/grpSp（跨兄弟操作稳定），再在该父容器子元素中按 cNvPr@id 线性查找匹配——找不到返回 ErrStaleHandle。**关键不变量**：句柄身份 = cNvPr@id，`MoveShape` 后句柄**仍有效**（cNvPr@id 未变），只有 `RemoveShape` 让 cNvPr@id 消失时句柄才失效。③ **idHint=0 路径回退**：零值句柄/老句柄走纯路径解析（保持向后兼容）。④ **文本节点（textNode）暂不修复**：没有等价稳定标识符（内容指纹会随正常编辑变化），README 明示其仍按 path 解析。⑤ **5 项专项测试**（create_test.go）：RemoveShape 后原 a 句柄 Name/ID/Bounds 全 ErrStaleHandle 但 b/c 句柄仍正常（cNvPr@id 身份稳定）；MoveShape 后句柄仍有效（关键不变量）；新构造句柄立即可用；Save+Close 后句柄返 ErrClosed | shapes.go（locate + locateByIDHint + 3 helper + classifyShape 改写）、create.go/audio.go/audiotiming.go/chart.go/picture.go/video.go 各 1 处 idHint 补全。**全量测试零回归**——这是非破坏性 fix：cNvPr@id 仍在文档中的句柄查找逻辑完全等价于"按 path 解析后 elementCNvPr 读 id 校验"，只有 cNvPr@id 消失时行为变化（以前是错配到相邻兄弟，修复后是 ErrStaleHandle）。gofmt/vet/全量测试 + CGO=0 Linux + js/wasm + wasip1/wasm 三交叉构建通过 |① **`wasm/check` 包**——导出纯函数 `Inspect(ctx, bytes, name)` / `Capability(name)` / `Validate(ctx, bytes, name)`，统一返回带 `ok/error` envelope 的 JSON 字符串（便于在 wasm/js 边界统一消费）；`AsError` helper 让 JS 端可用 `try/catch` 模式拿错误；`SchemaVersion()` 暴露 `go-pptx.capability/1.0`；`FailJSON` 用作 syscall/js 顶层 fallback（参数缺失等前置错）。所有计算在 wasm 线性内存完成——**文件不离本机**。② **`cmd/pptx_check` WASM 主入口（构建约束 `//go:build js && wasm`）**——`syscall/js` 注册 `GoPptxCheck.{schemeVersion, inspect, capability, validate}` 命名空间函数（始终返回 string 而非 Go map——避免 JS 端 JSON.parse 把 Go any map 序列化为 `"[object Object]"`）。**关键修复**：首版 wrap 在 map[string]any 时 JS 端解析失败。③ **`wasm/site/` 静态网页**——`check.html`（UI：文件选择、Inspect/Validate/Capability 按钮、隐私承诺卡片、维度状态小卡片+JSON 双视图）、`check.js`（`FileReader` 读取本地文件 → `Uint8Array` → `WebAssembly.instantiateStreaming(fetch('pptx_check.wasm'), Go.importObject)` → 调用 window.GoPptxCheck.*）。`smoke.cjs` 提供 node.js 烟雾测试（证明 wasm 加载+四函数调用+JSON 反序列化均通过）。④ **`scripts/check_wasm.{sh,ps1}` 构建脚本**——编译 pptx_check.wasm 至 `wasm/site/`、从 `$GOROOT/lib/wasm/wasm_exec.js` 拷贝 runtime、给出 `python -m http.server 8080` 用法提示。⑤ **`.gitignore`** 增加 `/wasm/site/pptx_check.wasm` 与 `/wasm/site/wasm_exec.js`（CI 每次重新构建避免二进制腐化）。⑥ **CI WASM 编译 job 已存在**——V2.6 §26 P1 评审要求 `CGO_ENABLED=0 GOOS=js GOARCH=wasm go build ./...`，本次新增 `cmd/pptx_check` 与 `wasm/check` 已一并通过；to-be-confirmed 后续接入到 CI yaml。⑦ **`pptx` 根包导出公共 helpers** `NewCapabilityManifest / PopulateCapabilityDimensions / PopulateCapabilityFeatures / SortCapabilityFeatures`—— `wasm/check` 复用而不必复制 enumerate 逻辑（CAP-01 在 `Presentation.Capability()` 与 `wasm/check.Capability()` 之间保持等价） | wasm/check/check.go（~250 行纯函数），wasm/check/check_test.go（11 用例：SchemaVersion 等价 SDK / Capability 无输入 manifest 完整六维 / Capability 无参数仍 valid / Inspect 真实字节 ok+pages/sdKversion 透传 / Validate 真实字节 structural 模式 / Inspect 错字节 envelope ok=false / Validate 错字节 envelope / 空字节不 panic / AsError 检测 / AsError ok 时 nil / Marshal 后 JSON token 验证 / 与 Presentation.Capability 等价性）+ cmd/pptx_check/main.go（WASM 注册 + 文件大小提示）+ wasm/site/check.html + check.js + smoke.cjs + scripts/check_wasm.sh + scripts/check_wasm.ps1。**质量门**：go test 全绿、go vet 干净、gofmt 干净、CGO=0 + Linux/js+wasm/wasip1+wasm 三交叉构建通过；**烟雾测试**通过 `/c/Users/32791/.workbuddy/binaries/node/versions/22.22.2-2/node smoke.cjs` 在 wasm/site/ 执行——`exposed: schemeVersion,inspect,capability,validate` + `schemeVersion: go-pptx.capability/1.0` + `capability.ok=true dims=6` + 错字节 envelope ok=false；**核心包 wasm/check 覆盖率 86.0%** |
| 2026-09-09 | **CAP-01 capability manifest 与 JSON schema（M7 首项）**：六维状态 `Inspect/Create/Edit/Preserve/Render/Play` 替代单一 FullySupported 标签，状态枚举 `Supported/Partial/Unsupported/Untested`（MarshalJSON 输出字符串 + 未知值兜底 Untested，向前兼容）。① **公共 API**：`Presentation.Capability(sourcePath)` 返回 `CapabilityManifest`，与 SaveReport/能力报告同事实来源；schemaVersion="go-pptx.capability/1.0"（独立于 SDK 版本）；只读不修改文件。② **数据结构**：`CapabilityDimension{Status, Notes, AppliesTo, Limits}` + `CapabilityFeature{Key, Name, Stage, TargetTier, Status, WorkPackage, Notes, Limits, AppliesTo}`；每条 Feature 对应 §24 一行工作包 + §2.3 一行特性（矩阵↔工作包追溯不断）；Source 字段含 Input/Size 不可访问时仍记录路径以便诊断；Features 排序为 Key 稳定字典序。③ **Capabilities（六维静态快照）**：Inspect=Supported（IR 全集 + R 档公共 API）、Create=Partial（六类显式 shape/audio/video/picture/chart/slide）、Edit=Partial（跨 Run 替换+属性 patch+受限播放）、Preserve=Supported（SaveReport+字节级保留+§15.3 硬门槛）、Render=Untested（§14 设计态推迟至后续立项）、Play=Partial（配音 E 受限 + timing P 档透传）；每维含 Notes/AppliesTo/Limits。④ **`pptx capability` CLI 子命令（cmd/pptx/capability.go）**：只读 + `--json` 标志位 + 拒绝 `--output`；输出缩进 manifest（含 source/dimensions/features/SDKVersion）；退出码遵循 §23.2（0 成功，3 能力限制）。⑤ **Capabilities 与 §2.4 护城河第 2 项对齐**：未实现的 M7/M8 工作包（TOOL-02/TIMIR-01/TPL-01/DIFF-01）显式 `Untested` + WorkPackage 字段，便于调用方基于 manifest 做能力感知降级。⑥ **JSON 编解码稳定**：`MarshalManifest`/`MarshalManifestIndent`/`UnmarshalManifest` 三 API；round-trip 校验维度键集与 schemaVersion 不丢失 | capability.go（~340 行，含六维状态枚举与 CapabilityStatus 序列化、Source 路径+大小捕获、populateCapabilityDimensions/populateCapabilityFeatures 两个独立函数便于 CLI 不加载 Presentation 构造默认 manifest；Features 按 Key 字典序稳定排序）+ capability_test.go（14 用例：schemaVersion 恒定/六维齐备/Render 设计态 Untested/Play 引用 AUDIO/Preserve 含 SaveReport 与字节级保留/Features 覆盖 10 个关键 WP/M7+M8 标记 Untested/JSON round-trip/Status 序列化与未知兜底/Source 路径与 Size/不存在源仍记录路径/Features 排序稳定/TargetTier 合法值含 R+E 复合/GeneratedAt RFC3339/newCapabilityManifest 独立可用/SDKVersion 链接器覆盖；不依赖 testdata/外部 fixture，使用 pptx.New() 自建最小模板） + cmd/pptx/capability.go + cmd/pptx/main.go 注册子命令。覆盖率 77.3% → **77.4%**；vet/gofmt/WASM(js+wasm, wasip1)/Linux CGO=0 三交叉构建通过；`pptx capability` CLI 烟雾测试通过 |
| 2026-09-09 | **M7 立项阶段登记**：docs/M7-排序输入.md 建立——锚定方案 §2.4 创新方向（CAP-01 护城河第 2 项 / TOOL-02 浏览器原生检查工具 / TIMIR-01 动画时序只读 IR / TPL-01 模板数据绑定）+ 实施计划 §12 候选排序，4/4 设计基线首选直接命中；§1.3 登记 M7 前置底座全部具备（AUDIO-02 / TOOL-01 / TEXT-02 / CHART-01 / GEOM-01/02 / STYLE-01/02）；CAP-01 进入检查 §5 通过 | 预排与方案完全对齐；testdata/corpus/ 待语料回填 |
| 2026-09-09 | **GEOM-02 几何/填充/效果 R 档只读报告（M6 第七项收尾）**：Shape 接口扩展三方法 `Geometry() / Fill() / Effects()` 均走 R 档——解析并输出诊断，不提供写入 API。① **GeometryInfo**：`a:prstGeom`（Preset+全部 `a:avLst/a:gd` Adjusts；按文档序保留 Unknown 兄弟元素但只把非 xfrm/fill/ln/effectLst/effectDag/scene3d/sp3d/extLst/style 之外的几何元素记 Unknown）+ `a:custGeom`（Guides 公式全集 + PathLst 每条路径的 w/h/fill/stroke 与全部命令 `moveTo/lnTo/arcTo/cubicBezTo（3 点）/quadBezTo（2 点）/close` 坐标），`Paths` 子树未知元素入 `Unknown`；**坐标点不在 a:custGeom 解析时做任何世界变换**（GEOM-01 路径的几何投影不在本档范围）。② **FillInfo**：`spPr/a:fill` 首元素分类——noFill/solidFill/gradFill/pattFill/blipFill/grpFill，复用 tablestyle.go 的 `FillKind`（`FillUnspecified` 兼作未识别类别语义，配 `Raw` 记录底层元素 Local 名，`FillPicture` 对应 a:blipFill）；`a:gradFill` 内 `a:gsLst` 停止点 pos+Color + `a:lin`（ang 1/60000 度+scaled）+ `a:path`（path=rect/circle/shape + fillToRect 四通道 l/r/t/b 千分比），缺 pos 走 `fill.gradient.invalid_pos` 诊断而非 0；`a:pattFill` 收 prst + fgClr + bgClr；`a:blipFill` 收 `a:blip@r:embed`（**关系命名空间 = nsOfficeDocument**——`c.Attr("", "embed")` 漏不到，吸收为已知易错点）+ `@dpi` + `@rotWithShape` Optional[bool] + `a:srcRect` 四通道。③ **EffectInfo**：`a:effectLst` 或 `a:effectDag` 内 outerShdw/innerShdw/glow/softEdge/reflection/fillOverlay + 公共 `@hideSelf`/`@blend`；外/内阴影 blurRad/dist/dir/sx/sy + algn 入 Unknown；glow/softEdge 共用 rad；reflection 收 blurRad/stA/stPos/endA/endPos/dir/fadeDir；fillOverlay 仅颜色；效果 Alpha 取自 color 解析后的 alpha 变换。`a:scene3d` 解析 camera（prst+zoom+rot lat/lng/rev）+ lightRig（rig+rot）+ backdrop@plane（Optional[bool]），`a:sp3d` 收 prstMaterial/extrusionH/contourW + bevelT/bevelB 的 prst/w/h；未知效果类别归 `EffectUnknown` 入 `effects.unknown_kind` Info 诊断。R 档——解析失败降级为 Diagnostic（`geom.spPr.missing` / `fill.spPr.missing` / `fill.unknown_kind` / `effects.spPr.missing` / `geom.custGeom.missing_pt` / `geom.custGeom.invalid_w/h`），从不返回 error（V2.6 §941 R 档契约）。**复用 tablestyle.go 的 FillKind 类型**避免重复声明，结构与现有 API 完全一致 | geomadv.go（~700 行 R 档解析器，含 parseShapeGeometry/parseShapeFill/parseShapeEffects 三个分支派发到子解析器 + parsePresetGeomAdjusts/parseCustomGeomPathList/parsePathCommands/parseSinglePoint/parseTriplePoints/parseGradFill/parsePattFill/parseBlipFill/parseScene3D/parseShape3D/parseBevel/axisTriplet/parseShadow/parseBlendModeAttrs/parseOptionalBool）+ geomadv_test.go（21 用例：prstGeom 直读/带 adjusts/custGeom 全命令路径与 guides/无 spPr 警告/spPr 无几何/solidFill/noFill/linearGradient 含 lin/gsLst/pathGradient 含 fillToRect/pattFill prst+fg+bg/blipFill rId+dpi+rotWithShape+srcRect/grpFill/无 a:fill 容器警告/未知 fill 类别 raw+diag/gradient invalid pos 诊断/outerShdw blurRad+dist+dir+sx+sy+algn+alpha/glow+softEdge 共存/reflection 全属性+fillOverlay/scene3d+sp3d 完整解析/无 effectLst 容器/FillInfo 不返 error）。shapes.go 修改 Shape 接口扩展 3 个 R 档方法（与已有 Line/StyleMatrixRefs 一致，全部由 shapeNode 基元实现）。覆盖率 76.9%→**77.3%**；vet/gofmt/WASM(js+wasm,wasip1)/Linux CGO=0 交叉构建通过；M6 七包全部完成 |
| 2026-09-09 | **STYLE-02 主题样式矩阵与颜色变换全集（M6 第六项）**：① 颜色变换补齐到 ECMA `EG_ColorTransform` **全集 28 种**——新增绝对量 `hue/sat/lum/red/green/blue`（val 单位与内部表示一致：hue 1/60000 度、其余 1/100000）与曲线 `gamma/invGamma`（`gamma`=pow(c,1/g)、`invGamma`=pow(c,g)，g=val/100000，g<=0 视为无操作）；越界 val 走 `clampPct`/`clamp8` 约束而非回绕。② **修复 `hslToRGB` 两处缺陷**（M3 遗留，影响全部 HSL 变换）：`conv` 闭包把结果写回共享变量 `p`，导致三通道串行污染；`hf` 误用 `hue/60`（量纲 0..6）而 hue2rgb 要求 0..1，应为 `hue/360`——修复后 `satMod/hueMod/hueOff/comp/hue/sat/lum` 结果正确（`hslToRGB(120°,100%,50%)=00FF00`）。③ 样式矩阵引用链新增 `a:fontRef`（`RefFont` + `ThemeFontSlot{major,minor}`，`idx` 支持 `major/minor/1/2`，解析到主题 `a:fontScheme` 的 latin→ea→cs 字体名，越界 idx 返回 `FontSlotUnknown` 不臆造）。④ 主题条目解析为可呈现颜色 `StyleMatrixRef.ThemeColor`——`phClr` 以引用方颜色代入为基色后套用主题条目自身变换（ECMA §20.1.2.3.23），引用方颜色未解析时 `RGB` 留空不臆造；`effectRef` 无颜色元素不产生诊断。R 档——解析并输出诊断，不提供写入 API | styleadv.go（phClr 代入 + 主题字体引用 + 主题条目节点定位）+ styleadv_test.go（13 用例：变换全集 28 项白名单正反校验、绝对量往返 11 例、越界约束、gamma/invGamma 含恒等与 g<=0 无操作、未知变换仅入 Unknown 不臆造、RefFont.String、fontRef minor/major、fontRef 数字 idx 1/2/3、phClr 代入 lumMod 50%、非 phClr 实色条目、引用方颜色缺失不臆造、effectRef 不产生颜色诊断）；format.go 修改 knownTransformKinds/applyColorTransforms/hslToRGB/MatrixRefKind/StyleMatrixRef/StyleMatrixRefs。覆盖率 76.4% → **76.9%**；vet/gofmt/WASM(js+wasm,wasip1)/Linux CGO=0 交叉构建通过 |
| 2026-09-09 | **LAYOUT-01 版式/章节/嵌入字体/讲义母版/避头尾规则（M6 第五项）**：R 档只读报告 `Presentation.LayoutInfo()` 返回 `LayoutReport`：① 章节 `p14:sectionLst`（`LayoutSection{ID,Name,Type,SlideIDs}`，仅根 sldIdLst 收集 used 集合——避免误计 section 内嵌 sldId，unknown sldId 引用记 `layout.section.broken_ref` 诊断）；② 嵌入字体 `p:embeddedFontLst`（按 master × typeface × 4 变体 regular/bold/italic/boldItalic，关系不可达记 `layout.font.broken_ref`）；③ 讲义母版 `p:handoutMasterIdLst` + 主关系 `RelHandoutMaster`（缺目标/未声明/未注册 r:id 记 `layout.handout.broken_ref`，新增 `opc.RelHandoutMaster` 常量；`HandoutMasterInfo{Part,RelationID,Present}`）；④ 母版 `p:txStyles` 文本样式的 `a:lang/altLang/kumimoji/kinsoku` 按 lang 文档序聚合（`KinsokuRule{Lang,AltLang,Kumimoji,KinsokuFlag,Parts}`）。四项均不提供写入 API（仅解析-输出诊断，符合 V2.6 §941 R 档语义"可解析并输出诊断，不提供写入 API"）；解析失败不返 error，降级为 Diagnostic；关闭的 Presentation 返回 ErrClosed + 空报告 | layoutdiag.go（~580 行 R 档解析器）+ layoutdiag_test.go（10 用例：空文档/Closed 守卫/Sections 往返/Sections broken sldId ref/EmbeddedFonts 4 变体往返/EmbeddedFonts 缺关系诊断/HandoutMaster 关系声明/HandoutMaster 缺目标诊断/Kinsoku 跨 titleStyle+bodyStyle+otherStyle 聚合/LayoutInfo 不修改原状态可重入）。internal/opc 新增 `RelHandoutMaster` 常量。覆盖率 76.3% → **76.4%**；vet/gofmt/WASM(js+wasm, wasip1)/Linux CGO=0 交叉构建通过 |
| 2026-09-09 | **CHART-02 图表标签/误差线/趋势线/轴扩展（M6 第四项）**：`ChartDataLabel{Show, Position 白名单 11 值（ctr/inr/inb/outT/outB/outL/outR/t/b/l/r）}`、`ChartErrorBars{Type 四值（stdDev/stdErr/fixed/percentage）/ Direction 三值（plus/minus/both）/ Value（Fixed/Percentage 必填与节流）/ NoEndCap(bool)}`、`ChartTrendline{Type 六值（linear/log/exp/poly/power/movingAvg）/ Period（MovingAverage≥2）/ Order（Polynomial∈[2,6]）/ DisplayEq/DisplayRSq/Name/SetIntercept+Intercept}`、`ChartAxisOptions{CategoryAsDate（c:dateAx 替代 c:catAx）/ ValueLogBase ∈ {0,2..32} / Min,Max Optional[float64] / Position（b/l/t/r）}`；`ChartSpec.DataLabel/Axes`、`ChartSeries.ErrorBars/Trendline` 公开字段接入；`buildChartSpaceXML` 在系列 `c:tx` 与 `c:cat` 之间写 trendline/errBars、在图表组按 OOXML 序（bar 在 ser* 与 gapWidth 之间、line 在 ser* 与 marker 之间、pie 在 ser* 与 firstSliceAng 之间）写 chart-level `c:dLbls`、`buildCatOrDateAxFragment`/`buildValAxFragment` 切分类别/日期轴并展开 `<c:scaling><c:logBase/></c:scaling>` 与 `c:min/c:max`；`chartIsCanonical` 接受新元素（系列 + trendline/errBars；图表组 + dLbls；plotArea + dateAx；valAx + logBase/min/max；catAx/dateAx + axPos 覆盖）；`parseChartSpace` 读回 ChartDataLabel/Trendline/ErrorBars/ChartAxisOptions；`SetData` 在带扩展字段时仍认定规范（图表/工作簿同步重建）；Pie 拒绝 ErrorBars/Trendline；Polynomial/MovingAverage 参数越界、logBase/Position/min/max 白名单外统一 `ErrInvalidArgument`；未知 kind/direction 同样 `ErrInvalidArgument`（不部分合并） | chartadv.go + chartfrag.go + chartadv_test.go（19 用例：DataLabel/Trendline×6/ErrorBars×4/Axes 对数+Min/Max/Axes 日期往返、SetData 接受扩展往返、外来 dLbls+trendline+errBars 整体识别为规范、11 用例白名单拒绝参数/Pie 拒绝）。chart.go 修改 ChartSpec/ChartSeries/ChartData 公共结构（向后兼容：未设字段 nil 指针）和 buildChartSpaceXML/parseChartSpace；覆盖率 75.9% → **76.3%**；vet/gofmt/WASM(js+wasm,wasip1)/Linux CGO=0 交叉构建通过 |
| 2026-09-09 | **TEXT-03 文本框高级项与字段全集（M6 第三项）**：`TextFrame.BodyProps()/SetBodyProps(BodyProps)`（`Optional[T]` 语义：仅 Set=true 字段被写入/清除，未提及 = 不动；R 档三字段 `numCol/vert/anchorCtr`；vert 6 值白名单 `horz/vert/vert270/wordArtVert/eaVert/mongolianVert`、未识别 ErrInvalidArgument；numCol <0 拒绝、=0 表示清除；anchorCtr 0/1 二态）；`a:bodyPr` 缺失时按 schema 序（txBody 首个子元素）自动新建并展开自闭合 txBody；自闭合 bodyPr 上插入属性走 `OpenEnd-2`（`/` 之前）正确锚点；`Paragraph.AppendField/InsertField(at)/Fields/Remove`（`a:fld` 白名单 `slidenum/datetime`；`FieldSpec{Kind,Guide,Text,Style}`；datetime `guide` 17 项白名单 `YYYY-MM-DD/hh:mm:ss/MM-DD-YYYY/...`、未识别 ErrInvalidArgument；slidenum 不接受 guide；未知 kind 整体 ErrUnsupportedEdit；**始终生成展开 `a:fld…><a:t>…</a:t></a:fld>` 形态**确保 PowerPoint 识别并按 guide 重新计算）；`Field.Kind()/Guide()/Text()/SetText()/Remove()`（Guide 兼容 PowerPoint 扩展属性 `fldGuide` 与旧 `guid`；SetText 在自闭合 fld 上自动展开为含 a:t 形态）；**Paragraph.Text() 含字段缓存文本展示**（a:br 仍不贡献、a:fld 贡献其 a:t 缓存文本；a:r 与 a:fld 按文档序拼接——TEXT-03 §7.1 字段展示规则固定）；runs/fld 互不渗透（Paragraph.Runs() 不含字段） | textadv.go + textadv_test.go（25 用例：BodyProps 读写/patch 语义/清除/无效 Columns 拒绝/全 6 vert 值白名单/bodyPr 自动创建/补自闭合 txBody；AppendField slidenum/datetime+Guide 读取；未知 kind + ErrUnsupportedEdit；非法 guide + ErrInvalidArgument；slidenum 带 guide 拒绝；Append+Insert+Remove 往返；Fields 空段落；InsertField after Run / nil anchor / stale run 拒绝；SetText 自闭合 fld 自动展开；SetText 含 & 转义往返；Paragraph.Text 含字段/含自闭合字段；Style 透传到 rPr）；text.go 修改 Paragraph.Text()；覆盖率 75.9% 持平；vet/gofmt/WASM(js+wasm, wasip1)/Linux CGO=0 交叉构建通过 |
| 2026-09-09 | **VIDEO-01 视频媒体形状（M6 第二项）**：`Slide.AddVideo(VideoSpec)` 嵌入 MP4/WebM 视频 Part（SHA-256+ContentType 去重；video/mp4 / video/webm CT 映射）+ video 关系（ECMA relType video，复用同目标同类型 rId）+ `p:pic + p:blipFill + a:blip r:embed + p:videoFile contentType` 片段（PowerPoint 接受的标准视频形状表达）+ spPr/a:xfrm/a:prstGeom=rect；VideoProfile 落 `/docProps/video.xml` 自有扩展（schema `https://schemas.example.org/F31/go-pptx/video/2026`，TrackKey/Role/MediaPart/SlidePart/ShapeID/SHA256/Version/PosterPart）；可选 PosterFrame（IMAGE-01 占位图片与视频同事务提交，`VideoShape.HasPoster()/PosterSource()` 访问，clone 时 PosterPart 同步重映射）；`VideoShape` 句柄（VideoSource 运行时回放、`Role`/`Profile`/Kind=ShapeVideo）；形状分类 `picMediaKind` 探测 p:videoFile → `ShapeVideo`/`a:audioFile` → `ShapeAudio`/普通 → `ShapePicture`，三分类接入 Shapes()/classifyShape；Clone 派生 TrackKey（-c/-c-N 唯一化）+ 目标 SlidePart + 独立媒体 MediaPart/PosterPart 重映射（profileAddsV/stageAppendVideoProfiles 同事务追加）；`internal/videoprobe` 包：ISO BMFF ftyp box 签名+品牌白名单（isom/mp41/mp42/dash/avc1 等）+ EBML 0x1A45DFA3 头签名 + DocType=webm/matroska 软验证；时长精确解析依赖 moov/mvex/track header（V2.6 不实现）属后续切分，调用方可传 Duration 或接受 ErrDurationUnknown | video.go (~660 行) + video_test.go (15 用例：MP4/WebM 嵌入与分类、重复 TrackKey/未知格式/空字节/零尺寸/路径分隔 TrackKey 拒绝、SHA-256 去重、Bounds 反映、VideoSource/PosterSource 字节往返、AltText+IsDecorative 互斥、Clone 派生 TrackKey + 视频形状、分类为 ShapeVideo 非 ShapePicture) + internal/videoprobe (11 用例：ftyp 签名/品牌白名单/截断宽容/非 ftyp 跳过、WebM EBML 签名/无 DocType 仍 webm、空数据/IsKnownContainer、Duration 毫秒向上取整)；覆盖率 75.6%→**75.9%**；vet/gofmt/WASM(js+wasm,wasip1)/Linux CGO=0 交叉构建通过 |
| 2026-09-09 | **ANIM-02 过渡动画基础子集（M6 首项）**：`Slide.Transition()/SetTransition(TransitionSpec)/RemoveTransition()` 三 API；白名单 8 个过渡类型 `none/fade/push/wipe/split/cover/cut/dissolve`（ECMA ST_TransitionType 受控子集）+ 容器属性 `spd`（slow/med/fast，缺省 fast）+ `advClick`（`*bool` 区分默认 true 与显式 false，缺失=默认值）+ `advTm`（由 SetAdvanceAfter 维护，SetTransition 保留不覆盖——避免与 AUDIO-02 计时计划双源）；`p:transition` 子元素仅识别白名单，**p14:morph 等未支持子元素整体拒绝 ErrUnsupportedEdit**（不做部分合并；p14 命名空间常量已就位）；新建容器按 CT_Slide 子元素序插在 `cSld/clrMapOvr` 之后、`timing` 之前；二次 SetTransition 走 in-place SpanPatch 替换不重复创建（容器计数恒为 1）；RemoveTransition 整段 SpanPatch 删除且不影响 `p:timing` 树（按 §24 ANIM-02 验收"不破坏原 timing 树"） | transition.go/transition_test.go（17 用例：none/fade+throughBlack/push+dir/wipe+dir/split dir+axis/dissolve/cut/cover 写入读取、spd 规范化、advClick 默认与显式、advTm 与 SetAdvanceAfter 协同、in-place 替换、未知 type/dir/spd 拒绝 ErrInvalidArgument、p14:morph 注入整体拒绝 ErrUnsupportedEdit、Remove + RemoveNoOp、与 p:timing 共存、stale/closed 守卫、保存往返）；覆盖率 75.7%→**75.6%**；vet/gofmt/WASM(js+wasm,wasip1)/Linux CGO=0 交叉构建通过 |
| 2026-09-09 | **M6 排序输入登记**：docs/M6-排序输入.md 建立——锚定方案 §2.3"过渡动画/视频形状/嵌入字体/颜色变换全集"为 M6 高频首选；§12 候选排序 ANIM-02/VIDEO-01/TEXT-03/CHART-02/LAYOUT-01/STYLE-02/GEOM-02（预排第一=ANIM-02）；§1.3 登记本仓库已落地能力与 M6 工作包衔接点（SetAdvanceAfter→ANIM-02 容器、ChartWorkbookBuilder→CHART-02、applyColorTransforms→STYLE-02、cloneWalkContainer→跨文档复制）；§2.2 重排窗口（语料 ≥10 份回填/客户端反馈/依赖阻塞/ADR）；§5 ANIM-02 进入检查通过 | 预排可被语料回填推翻（testdata/corpus/ 仅 README 无样本）；§12 §R7 缓解措施已登记 |
| 2026-09-09 | CLONE-01 同文档受限页面复制（M5 第二项）：**两阶段实现**——`buildClonePlan` 纯读取验证（任何拒绝发生在暂存前，零残留）+ `applyClonePlan` 单事务提交（snapshotPending 快照，暂存失败恢复 pending）；**依赖闭包** `cloneWalkContainer` 递归 slide→notesSlide/chart→workbook，remap 入口登记天然防循环（visited）；**关系分类** `classifyCloneRel` 按容器（slide/notes/chart）区分：slideLayout/notesMaster 复用（目标存在性校验）、notesSlide/chart 独立克隆递归、image/audio/video/media 按 ClonePolicy.IndependentMedia（默认共享同一媒体 Part）、外部关系逐字节保留（Target/TargetMode 原样）、其余内部类型 **ErrUnsupportedEdit 整体拒绝**（AT-13 同文档口径：OLE/SmartArt/外部图表颜色样式 Part 等，不生成残缺目标页）；**引用重写** `patchedCloneRels`——克隆容器关系流为全新 Part，逐条保留 rId/Type/TargetMode/转义原文，仅对命中 remap 的内部关系 SetAttrValuePatch 重写 Target（`retargetRel` 同目录前缀保持），slide XML 字节级复制（r:embed/r:link 恒等、ShapeID/timing 节点 ID 均为页内作用域恒等成立）；notesSlide 回引校验必须指向源页并重写到目标页；**名字分配** `allocCloneName` 数字尾名（chart1.xml→chart2.xml）走 nextPartSeq 递增 + 事务内去重，非数字尾名 stem-N 后缀；**Profile 克隆** 库创建音轨按页聚合，派生 TrackKey（-c/-c-N 唯一化）+ SlidePart 指向目标页 + 媒体独立时 MediaPart 同步重映射，同事务追加 /docProps/audio.xml；**注册** appendSldIdPatch + allocSlideID + 主关系 insertRel（与 AddSlide 同路径） | clone.go + clone_test.go（9 用例：基本结构（slide 字节级相等/chart 关系指向克隆 Part/版式复用不复制）、**图表数据隔离金样**（克隆后改 SetData，源 chart+workbook 字节不变且 Data 读取无泄漏）、媒体默认共享（单 Part 双引用）/IndependentMedia（字节相等副本+Profile MediaPart 重定向）、notes 克隆与回引重写（源 notes 不动、notesMaster 复用）、**未知关系拒绝**（注入 oleObject 关系→ErrUnsupportedEdit+无残留目标页）、TrackKey 派生（两次克隆互不冲突、源 Profile 原样）、保存往返（重开后 2 页+图表可读+备注+Profile）、stale/closed 守卫、外部超链接逐字节保留含 TargetMode）；覆盖率 75.7% 持平；vet/gofmt/WASM(js+wasm,wasip1)/Linux CGO=0 交叉构建通过 |
| 2026-09-09 | CHART-01 受限三类图表（M5 首项）：**图表创建** `Slide.AddChart`（ChartSpec：Type/Title/Categories/Series/EMU 几何）生成 `/ppt/charts/chartN.xml`（规范布局 c:chartSpace：barChart[barDir=col/grouping=clustered/gapWidth]·lineChart[marker/smooth=0]·pieChart[varyColors/firstSliceAng]、catAx+valAx 成对 axId=100000001/100000002（饼图无轴）、ser 的 tx/cat/val 三缓存 strRef+strCache/numRef+numCache、引用 Sheet1!$A$2:$A$n / $B$2:$B$n）+ 嵌入工作簿 Part（`/ppt/embeddings/Microsoft_Excel_WorksheetN.xlsx`）+ chart→package 关系 + slide→chart 关系 + spTree 末尾 p:graphicFrame（c: 前缀内联声明）；数据校验（≥1 系列/≥1 类别/长度相等/数值有限/WH>0，失败零残留）；**工作簿适配接口** `ChartWorkbookBuilder`（Build(ChartDataBook) 可替换，`SetChartWorkbookBuilder` 挂到 Presentation，空输出拒绝）+ `DefaultWorkbookBuilder` 纯标准库最小 xlsx（inlineStr 无 sharedStrings、确定性输出、行 1 系列名/行 2.. 类别+数值与图表引用约定配套）；**读取** `ChartShape.Data()`（分类 shapes.go 图形框→ChartShape/ShapeChart；chartSpace→chart→plotArea 首个受限图表组（组合图取首组）、title 富文本 a:t 拼接、系列名 strRef/strCache 或 c:v、类别 strRef/numRef/strLit/numLit 四路、数值 numCache/numLit，pt 按 idx 定位）；**受限 SetData**（AT-10 代码级等价）：结构白名单校验 `chartIsCanonical`（chartSpace 仅 c:chart、chart 子元素五种、plotArea 单图表组+成对轴、ser 子元素按类型白名单、标题富文本骨架）——外部/被改写图表 **ErrUnsupportedEdit 拒绝部分合并**、类型变更拒绝、必须存在嵌入工作簿；通过后 chart Part 与工作簿**同一数据快照重建**（Part 名与关系不变、缓存与工作簿逐格一致、引用范围随类别数扩展）、单事务提交 revision+1、shape id 不变 | chart.go/chartbook.go + chart_test.go（9 用例：柱状结构与双侧关系与保存 CT Override、折线/饼图结构与双图表独立工作簿、入参校验零残留、Data 往返含 XML 转义标题与系列名、**SetData 缓存/工作簿/引用范围三侧一致**、外来布局（ser 注入 dLbls）拒绝且 Part 字节不变、类型不匹配拒绝、自定义 builder 两次调用与空输出拒绝、保存重开 Data 完整、closed 错误）；覆盖率 75.4%→**75.7%**；vet/gofmt/WASM(js+wasm,wasip1)/Linux CGO=0 交叉构建通过 |
| 2026-09-08 | M4 配音功能包（MEDIA-01+AUDIO-01/02/03）：**媒体探测** internal/audioprobe——WAV/PCM 遍历 chunk（含奇偶填充与 ds64）+ MP3（MPEG1/2/2.5 Layer III、ID3v2/ID3v1 跳过、Xing/VBRI VBR 头、CBR 帧扫描），三级时长来源、Duration Known/Unknown 状态、损坏样本产生诊断不臆造；**音频嵌入** `Slide.AddAudio`（probe 探测或 CallerProvided 时长、未知时长拒绝 ErrDurationUnknown、SHA-256 去重媒体 Part、p:pic 形状 + a:blip/a:audioFile + slide 关系、重复 TrackKey 拒绝）；**AudioProfile 元数据**落自有命名空间扩展 `/docProps/audio.xml`（TrackKey/Role/MediaPart/SlidePart/ShapeID/Duration/StartDelay/Trigger/SHA256/Version，读写两侧共享 profileXMLAttrs/parseAudioProfile）；**受限播放树编辑**（AT-05）：`SetPlayback` 把 StartDelay/Trigger 记入 Profile 后按 slide 全部音轨幂等重建纯音频 `p:timing`（结构 tnLst/par/cTn tmRoot/audio/cMediaNode/cTn stCondLst cond delay/spTgt spid，时间节点 ID=900000+ShapeID*2），既有 timing 含非白名单元素（动画/视频等）→ ErrTimingConflict 不做部分合并；**计时计划**（AT-06/07/12）：`PlanTimingSync` 计算 Ej=StartDelay+Duration、AdvanceAfter=max(Ej)+TailPadding（纯计算不修改文档），参与集合=Narration+OnSlideEnter；UnknownDuration 默认 Fail 整批拒绝（Skip 可跳过并报告）；`ApplyTimingPlan` revision 校验拒绝（ErrConcurrentModification）+ `SetAdvanceAfter` 写 p:transition@advTm（缺失时按 CT_Slide 子元素次序在 timing 前创建）；**幂等旁白**（AT-08）：`UpsertNarration` 返回 added/unchanged/updated（SHA-256 字节比对），Version 自增、shape id 不变 | internal/audioprobe/（probe/wav/mp3/dispatcher，11 用例）+ audio.go/audio_test.go（6 用例）+ audiotiming.go/audiotiming_test.go（10 用例：SetPlayback 结构与幂等/复杂 timing 冲突/transition 创建与原地更新/UpsertNarration 三态与 Version/**AT-06 数值验证 10s@0+8s@6s+尾 1s→15s 且 advTm=15000 落盘**/AT-07 未知时长 Fail 与 Skip/AT-12 revision 失配/SlideByID）；**关键修复**：① p:timing/p:transition 是 p:sld 根级子元素（非 spTree 内），插入位置按 CT_Slide 次序；② 旧版 SetPlayback 片段含 `\"` 反斜杠转义产生非法 XML（背引号字符串内不转义）→ 全部重写为结构化生成；③ handleByShapeID 漏 nvPicPr 间接层（cNvPr 不是 p:pic 直接子元素）→ 改用 elementCNvPr；④ recordAudioProfile 首建后再次 stagePatch 的 Added/Patched 交叠（复用 commit 修复）；Profile 数字属性必须引号包裹（%q）否则 xmlstore 报 unquoted attribute。覆盖率 75.6%→**75.4%**（新分支多）；vet/gofmt/WASM(js+wasm,wasip1)/Linux CGO=0 交叉构建通过 |
| 2026-09-08 | 格式深度子集（M3 第三项/最后项）：**颜色变换全集** `applyColorTransforms` 19 种变换（lumMod/lumOff/shade/tint/{red,green,blue}{Mod,Off}/satMod/satOff/hueMod/hueOff/alpha/alphaMod/alphaOff/inv/gray/comp，整数除法 val/100000）；**线条系统** `shape.Line()` 解析 `a:ln`（w/cap/cmpd/algn/prstDash/custDash/round/bevel/miter/headEnd/tailEnd + 颜色+Unknown 子元素）；**段落属性** `Paragraph.Props()` 解析 `a:pPr`（lvl/algn/indent/marL/marR/rtl/eaLnBrk/latinLnBrk/hangingPunct/fontAlgn/defTabSz + lnSpc/spcBef/spcAft（pct/pts）+ tabLst/tab + buNone/buChar/buAutoNum/buBlip/buFont/buSzPct/buSzPts）；**Run 高级属性** `TextRun.AdvancedProps()` 解析 `a:rPr`（baseline/spc/cap/strike/u/lang/altLang/kern/dirty/spellErr + highlight/sym + 已知但本包不展开项）；**主题样式矩阵引用链** `shape.StyleMatrixRefs()` 解析 `a:spPr/a:style` 内 fillRef/lnRef/effectRef（idx 1 基+颜色变换），`themeMatrixEntry` 沿主题 fmtScheme 的 fillStyleLst/lnStyleLst/effectStyleLst 定位；**Shape 接口扩展** Line/StyleMatrixRefs 加入公共面（全部实现共享 shapeNode） | format.go/format_test.go（12 用例：颜色变换 5 种组合+整数除法截断验证、线条系统全属性+Unknown 子元素、段落属性全属性+缺失 pPr、Run 高级属性+空 rPr、样式矩阵引用解析+idx 越界 unresolved、prstClr 已知色+未知变换触发 STYLE_PARTIAL、关闭句柄 ErrClosed）；**修复**：minimalTemplateParts 主题 clrScheme 已含 accent1-6 但 fmtScheme 三槽位只有 phClr 占位（idx 越界 unresolved），fixture 注入 3 项 fillStyleLst/lnStyleLst/effectStyleLst；fixture bytes.Replace 必须保留 `<a:fmtScheme ` 末尾空格避免属性粘连；覆盖率 76.9%→**75.6%**（新增分支多）；vet/gofmt/WASM(js+wasm,wasip1)/Linux CGO=0 交叉构建通过 |
| 2026-09-08 | TABLE-01 富文本表格、合并、样式子集（M3 第二项）：`TableShape` 句柄（含 `a:tbl` 的 p:graphicFrame 从 OpaqueShape 升级，`ShapeKind` 新增 ShapeTable；几何经 GEOM-01 的 p:xfrm 路径）；**逻辑网格映射**（DOM 顺序为每个 `a:tc` 分配格位，gridSpan/rowSpan 占据后续格，continuation（hMerge/vMerge）认领锚点覆盖区格位，不按 DOM 序号猜测）；`Cell` 句柄（富文本复用 TEXT-01 的 TextFrame、IsMerged/IsContinuation/Spans）；`Merge(CellRange)`（矩形校验 + **既有合并跨出区域边界拒绝 ErrUnsupportedEdit** + **AT-09 多非空单元格默认拒绝**、可 `WithMergeTextPolicy(MergeKeepAnchorText)` 明确保留锚点并清空其余正文；锚点写 gridSpan/rowSpan、覆盖格写 hMerge/vMerge，单事务提交）；`Unmerge`（清除跨度与 continuation 标记，不承诺恢复文本）；`RowHeight/SetRowHeight`、`ColumnWidth/SetColumnWidth`（a:tr@h / a:gridCol@w，负值与越界拒绝）；**样式子集**：三态区域开关（a:tblPr 的 firstRow/bandRow/firstCol/lastCol/lastRow/bandCol，on/off/def）+ tableStyleId、12 个 band/first/last 区域标志**优先级矩阵**（角单元格 > 首末行列 > 条带 > wholeTbl，由测试固定）、tableStyles.xml 解析、`EffectiveCellStyle` 逐属性返回填充/四边框/文本（对齐与边距）的 `ResolvedValue` 与来源链、**未知 styleId 或缺失定义 → unresolved + 诊断不臆造内置映射** | table.go/tablestyle.go/table_test.go（11 用例：分类与尺寸+几何、单元格读写、横向/纵向合并与 continuation、AT-09 拒绝与保留锚点策略、跨边界拒绝、取消合并、行高列宽与非法值、显式样式优先与来源、样式库区域优先级（nwCell>firstRow>wholeTbl）与未知 ID unresolved、生命周期、保存往返持久化）；**修复**：xmlstore.SetAttrValuePatch 仅支持已存在属性 → 新增 setOrAddAttr（精确替换或开标签追加）；样式库节点必须用其自身 doc 遍历（误用 slide doc 会解析出空颜色）；tcStyle 的填充在 `a:fill` 容器内、边框颜色在 `a:solidFill` 内需先进入；覆盖率 77.0%→76.9%（新增分支多）；vet/gofmt/WASM(js+wasm,wasip1)/Linux CGO=0 通过 |
| 2026-09-08 | GEOM-01 单位、组矩阵、四角边界（M3 首项）：`EMU int64` 单位类型（`EMUFromInches/EMUFromPoints` 舍入+溢出检查、`Inches()/Points()` 反向）+ `Point/Rect/Quad` 几何值类型（Quad 四角固定 TL/TR/BR/BL 顺序、`Rect.Contains`、`Quad.aabb`）；3×3 仿射矩阵（**列向量**，`affineCompose` 先右后左）；xfrm 解析（grpSp→grpSpPr/a:xfrm 含 chOff/chExt、graphicFrame→p:xfrm、其余→spPr/a:xfrm；rot=1/60000 度、flipH/flipV；**负坐标合法**、ext 非正/缺属性→ErrMalformedPackage）；**组映射 Mgroup=T(C)·R·F·T(-C)·G**（G=T(off)·S·T(-chOff) 非等比缩放；中心 C=off+ext/2；**chExt 零拒绝除法** ErrMalformedPackage 不默认 0）；**嵌套组父矩阵左乘**（自身翻转旋转在自身坐标阶段组合、祖先组自近向远左乘）；`Shape` 接口新增 `Bounds()`（本地框=直接父坐标 off/ext，不含翻转旋转与祖先组）/`WorldQuad()`（世界=页面坐标四角）/`WorldAABB()`；**`GroupShape` 正式句柄**（grpSp 从 OpaqueShape 升级，`Children()` 组内 z-order 枚举+嵌套组递归） | geom.go/geom_test.go（12 用例：单位换算含溢出/负值、顶层 Bounds=WorldAABB、**rot 正方向=顺时针（y 向下坐标系，ECMA ST_Angle 语义，金样待语料确认）**、flipH 绕中心镜像、组等比/非等比缩放映射 §8 公式、组 flipH 在 G 之后应用、**非等比组缩放×子自身旋转组合顺序**、嵌套组左乘数值、零 chExt 三态（组 Bounds 可用/子 WorldQuad 拒绝/子本地 Bounds 可用）、无 xfrm ErrNotFound、Children 嵌套枚举、stale/closed、Save 往返保真）；Shape 接口扩展（全部实现共享 shapeNode 基元无破坏）；覆盖率 76.3%→**77.0%**；vet/gofmt/WASM(js+wasm,wasip1)/Linux CGO=0 交叉构建通过 |
| 2026-09-08 | 页面 API 收口（M2 收口行）：`Slide(index)`（0 基越界 ErrOutOfRange）、`Slides()` 改读视图（会话新增 slide 立即可见，含 rId 内部校验）、`Layouts()`（沿主 Part→slideMaster→slideLayout 真实关系收集 LayoutRef，去重）、`AddSlide(layout)`（新建 slide Part+CT+自身 rels（→layout）+sldIdLst 注册+主 rId；**id 在 [256,2^31-1] 取最小空闲、耗尽 ErrLimitExceeded**；含自闭合/缺失 sldIdLst 展开；LayoutRef 绑定文档，跨文档 ErrForeignReference、nil ErrInvalidArgument、stale ErrNotFound）、`MoveSlide(id,index)`（**index=最终列表目标位置语义**；整元素字节搬移（删除+零长插入两组不重叠补丁）保真、未知子元素原地保留、no-op 不变更 revision）、`RemoveSlide(id)`（移除 sldIdLst 条目+主关系、删除 slide Part 与其 .rels、**连带删除关联 notesSlide（notesMaster 保留）**、**未知 Part 引用阻止删除 ErrUnsupportedEdit**、媒体等共享资源不 GC） | pages.go/pages_test.go（9 用例：下标越界/模板 Layouts/AddSlide 往返持久化与双加页/跨文档与关闭错误/MoveSlide 前移后移与 no-op/删除中间页持久化/备注连带删除/未知引用阻止/句柄失效）；**修复 nextPartSeq 不扫会话新增致同会话重复建 Part 撞名（notes 路径潜在 bug）** |
| 2026-09-08 | 页面 API 收口（M2 收口行）：`Slide(index)`（0 基越界 ErrOutOfRange）、`Slides()` 改读视图（会话新增 slide 立即可见，含 rId 内部校验）、`Layouts()`（沿主 Part→slideMaster→slideLayout 真实关系收集 LayoutRef，去重）、`AddSlide(layout)`（新建 slide Part+CT+自身 rels（→layout）+sldIdLst 注册+主 rId；**id 在 [256,2^31-1] 取最小空闲、耗尽 ErrLimitExceeded**；含自闭合/缺失 sldIdLst 展开；LayoutRef 绑定文档，跨文档 ErrForeignReference、nil ErrInvalidArgument、stale ErrNotFound）、`MoveSlide(id,index)`（**index=最终列表目标位置语义**；整元素字节搬移（删除+零长插入两组不重叠补丁）保真、未知子元素原地保留、no-op 不变更 revision）、`RemoveSlide(id)`（移除 sldIdLst 条目+主关系、删除 slide Part 与其 .rels、**连带删除关联 notesSlide（notesMaster 保留）**、**未知 Part 引用阻止删除 ErrUnsupportedEdit**、媒体等共享资源不 GC） | pages.go/pages_test.go（9 用例：下标越界/模板 Layouts/AddSlide 往返持久化与双加页/跨文档与关闭错误/MoveSlide 前移后移与 no-op/删除中间页持久化/备注连带删除/未知引用阻止/句柄失效）；**修复 nextPartSeq 不扫会话新增致同会话重复建 Part 撞名（notes 路径潜在 bug）** |
| 2026-09-08 | 形状枚举与 AltText(8.1) AutoShape（M2 收口行）：`Slide.Shapes()`/`Slide.Placeholders()`（spTree 直接子元素 z-order 枚举，跳过 nvGrpSpPr/grpSpPr，组内子形状不展开；`Shape` 公共面 ID/Name/Kind/AltText/IsDecorative；分类 sp→AutoShape[pic→PictureShape 其余→OpaqueShape 按元素 kind]）；`AutoShape` 句柄（cNvSpPr@txBox=1 判别 TextBox、`TextFrame()` 读写正文、`Placeholder()` 返回规范化 type/idx（缺省 obj/0，ECMA））；`OpaqueShape` 只读通用面；**§8.1 在 AutoShape 落地**（SetAltText/SetDecorative 与 PictureShape 共用 shapeNode 基元：写文本清 decorative、装饰性标记清除 descr、空串清 descr 不写装饰、读取两未声明→空值不臆测）；保存往返保真；形状句柄 ErrClosed/ErrStaleHandle 语义 | shapes.go/shapes_test.go（6 用例：枚举序/元信息、§8.1 互斥语义、往返持久化、TextFrame 读写与无正文 ErrNotFound、占位符讲稿读取闭环、生命周期）；PictureShape 重构嵌入 shapeNode（既有 image 测试全绿）；根包覆盖率 76.0%→76.3%；vet/全仓测试/WASM(js+wasip1)/Linux 交叉构建通过 |
| 2026-09-08 | CORE-01 建仓首批：go.mod（占位 module `go-pptx`，go 1.24.0）、LICENSE（MIT 默认）、.gitignore、README、CI（三 OS × 1.24/1.27 + WASM 编译 job + race） | 正式 module path/许可待组织确认后替换 |
| 2026-09-08 | CORE-02：module path 正式化 `github.com/F31/go-pptx`；许可证切换为 Apache-2.0（与远程仓库 LICENSE 字节一致）；关联远程 origin `https://github.com/F31/go-pptx.git` | 远程 Initial commit（仅 LICENSE）已并入本地历史 |
| 2026-09-08 | 根包 pptx 落地：稳定错误码全集（§20.4）、SlideID/ShapeID、Diagnostic/ValidationReport | errors.go/ids.go/diagnostics.go |
| 2026-09-08 | OPC-01 首批：`internal/opc` ZIP 条目索引（Scan）、PartName 校验、Budget 预算、实际字节计数读取（countedReadCloser）+ 测试 | 路径/重复/超限/大小写重复/恶意名用例 |
| 2026-09-08 | XML-01 首批：`internal/xmlstore` 命名空间感知词法扫描器（span/属性实体解码/注释/CDATA/PI/DOCTYPE/闭合校验/非 UTF-8 拒绝）+ 测试 | 节点索引树为后续任务 |
| 2026-09-08 | XML-01 节点索引树：`XMLDocument`/`NodeRecord`（Source/OpenEnd/CloseStart 跨度、属性记录、子元素序）、`NamespaceScope` 前缀解析+默认 ns+遮蔽/回退、mc/高频 ns URI 常量、未知子树与任意 ns 一视同仁建树、`Elements`/`Attr` 查询、深度预算（ErrDepthLimit，默认 256） | index.go/namespace.go；覆盖率 83.8%；等价前缀异写同 URI 识别用例 |
| 2026-09-08 | XML-02 补丁与受控插入：`SpanPatch` 区间替换（统一校验+升序重建、重叠/范围/锚定冲突检测 ErrPatch*）、`EscapeText`/`EscapeAttrValue`（XML 1.0 非法字符显式拒绝、空白属性逐字保留）、`SetAttrValuePatch`/`NewTextPatch`、`InsertBefore/After/AppendChild`（片段单根 well-formed + 前缀自足或插入点作用域可解析，否则 ErrFragmentNamespace；自闭合 AppendChild 拒绝） | patch.go/insert.go；覆盖率 86.2%；含垂直验证单元级雏形 TestPatchPreservesUntouchedRegions（改一个 Run，动画分支/未知子树字节逐字不变） |
| 2026-09-08 | 语料清单模板：`testdata/corpus/README.md`（QA-01 起步，24 条目标清单） | 待落实样本与许可 |
| 2026-09-08 | OPC-02 关系图/Content Types/主 Part 发现（M1 首项）：`ParseContentTypes`（Override 精确→大小写兜底→Default；重复/缺属性拒绝）、`ParseRelationships`（rId 唯一、目标相对源 Part 解析、越出包根拒绝、TargetMode 缺省 Internal + 绝对 URI 自动 External、百分号解码）、`Package.Load`（装配 + 全部 .rels 解析 + 双源冲突检测）、`MainPart`（officeDocument 关系、非固定名称）、`RelatedParts`/`RelatedByIDs`/`Walk`（visited 防循环） | package.go/relationships.go/contenttypes.go；非固定名称/循环关系/Override 金样三项验收均过 |
| 2026-09-08 | MODEL-01 公共 API 骨架：`Presentation`（New/Open/OpenReader/Save/Write/Close/Validate）、`Slide` 受控句柄、ErrClosed/ErrStaleHandle 语义、revision 事务骨架（stagePatch/stageDelete/commit、保存计划 revision 快照 + ErrConcurrentModification 守卫）、库内最小合法模板（presentation+master+layout+theme+docProps，原创生成无第三方素材）、原位保存拒绝（同路径/同文件实体） | presentation.go/slide.go/template.go；根包覆盖率 70.7%；M1 代码项全部完成 |
| 2026-09-08 | SAVE-01 保存计划与未变 Part 复制：`BuildSavePlan`（ChangeSet{Patched/Added/Deleted} → 唯一 PlannedEntry 清单；交叉冲突/存在性/名称校验）、CT 与变更集同源再生成（确定性序列化）、删除自动连带关系流、悬空关系 OPC_DANGLING_REL 诊断、`SavePlan.Write`（CopyOriginal 复制源解压内容，条目按名排序）；**B1 哈希回归全绿**（空变更集逐 Part 哈希一致 = AT-01 等价、补丁保存仅目标 Part 变化、增删后其余 Part 一致） | saveplan.go + contenttypes.go 写侧；opc 覆盖率 86.2%；原子落盘/失败恢复属 SAVE-02 |
| 2026-09-08 | SAVE-02 原子保存与失败恢复：`SavePlan.SaveToFile`（目标检查 → 同目录临时文件 → 写入 → fsync(可选) → Close → 输出校验（ZIP 结构+条目集一致）→ 原子替换（POSIX rename / Windows MoveFileEx REPLACE_EXISTING）→ 目录 fsync(可选)）；默认禁覆盖（ErrOutputExists），`WithOverwrite(true)` 显式启用；`WithDurability(DurabilityFull)` 文件+目录 fsync；替换失败保留旧目标 ErrAtomicReplaceUnavailable，**绝不删旧再写**；提交前失败清理临时文件（清理失败 errors.Join 附加不掩盖主错误）；writer 故障注入（zip.Writer 全程缓冲、Close 单次落盘） | save.go；**AT-11 等价用例全绿**（写入失败/目录不可写/默认禁覆盖三态旧目标不变、零临时残留）；opc 覆盖率 84.0% |
| 2026-09-08 | TEXT-01 富文本模型与备注（M2 首项）：DocumentStore 扩展（addedParts/deletedParts/partDocs 三表、`docOf` 泛型替代仅主 Part 视图、commit 合并三类变更整体失效缓存）、`Optional[T]`/`FontStyle`/`ColorSpec` 字体模型、TextFrame/Paragraph/TextRun 三级受控句柄（nodeStep 稳定路径定位）、`SetPlainText`（结构替换保 bodyPr/lstStyle、未知扩展拒绝）、`SetText`/`AddRun`、`SetFont`/`ResetFontProperty`（rPr 自闭合展开、属性/子元素精确补丁、空 rPr 自动移除）、备注四 API（SpeakerNotesText/SpeakerNotes/EnsureSpeakerNotes/SetSpeakerNotes，notesMaster 依赖真实关系解析）、Save/Write/Validate nil ctx 兜底 | presentation.go 扩展 + font.go/text.go/notes.go/text_test.go；**修复 xmlstore 属性删除边界 bug（AttributeRecord 补 NameStart/NameEnd，删除含闭合引号）与 pathToRun 缺 r 步骤**；根包覆盖率 49.1%；WASM/Linux 交叉构建通过 |
| 2026-09-08 | TEXT-02 跨 Run 替换与整批变更（M2 第二项）：`Paragraph.ReplaceText`（大小写敏感/字面/左到右非重叠/初始快照/不递归；空 old 拒绝；replacement 含换行拒绝）、三种格式策略 `ReplaceMode`（FirstCharacter 默认继承命中首字符 Run 格式；EqualLengthPerRune 逐 Run 等长文本替换不拆 Run；ExplicitStyle + WithReplacementStyle）、逻辑文本视图与 Text() 一致（rune 索引）、块边界（br/fld 硬边界 + 不同链接/动作 key 不可跨越）、字素簇保护（组合字符/ZWJ 序列边界拒绝）、安全保真（重建/删除 Run 前检查未知直接子元素与链接；unsafe/相邻共享 Run 冲突记 skipped 诊断）、`ReplaceResult`（Matches/Replaced/Skipped/Hits 段落 rune 定位）、`TextFrame.ReplaceText` 遍历段落；**占用 Run 区间贪心调度 + 同 Run 多命中合并单补丁 + 一次事务批量提交** | replace.go/replace_test.go；修复 `locateBlockSpan` 终点判定（gei<=hi）、`OperationError.Error()` 遗漏 Message 字段；根包覆盖率 49.1%→57.3%；vet/全仓测试/WASM/js/Linux 交叉构建通过 |
| 2026-09-08 | docProps 文档级元数据（5.1，V2.6 P0，M2 附加项）：`CoreProperties`（Title/Subject/Author/Company/Keywords/Category/Comments Optional[string] + Created/Modified Optional[time.Time]）、`CorePropertiesPatch`、`CoreProperties()/SetCoreProperties`；字段→XML 映射（Title→dc:title、Subject→dc:subject、Author→dc:creator、Keywords→cp:keywords、Category→cp:category、Comments→dc:description、Created/Modified→dcterms:* W3CDTF；Company→扩展属性 app.xml Properties/Company 非 core 标准字段）；**Optional patch 语义不清空未提及字段**；**Modified 默认库代管（保存时自动刷为当前 UTC，显式传入后以其为准、每次调用独立判断）**；core.xml 缺失时读取返回零值 nil、写入按需创建 Part+包根 rel+Content Type（随 Added 由保存计划生成 Override）；`CustomProperties()/SetCustomProperty`：lpwstr/i4/bool/filetime 四变体（CustomPropertyKind+构造器），同名覆盖保留 pid、pid=max+1 起始 2、fmtid 固定 GUID，i4 超 int32 拒绝，未知复杂变体读取 ErrUnsupportedFormat；custom.xml 缺失按需创建 | docProps.go/docProps_test.go（10 用例：默认读取/缺失零值/全字段往返含转义/补丁不清除/显式 Modified 保活/自动刷新/每次调用语义/custom 往返含覆盖/custom 缺失空 map/closed/未知变体）；**修复 commit 对会话新增 Part 再次修改丢失（addedParts.Content 未同步，Added 优先于 Patched 致旧字节输出）**；插入用多根手工 SpanPatch 分组锚定（xmlstore 受控插入拒多根）；根包覆盖率 75.2%→76.0%；vet/全仓测试/WASM(js+wasip1)/Linux 交叉构建通过 |
| 2026-09-08 | IMAGE-01 PNG/JPEG 图片与媒体暂存（M2 第四项）：`MediaSource` 契约（Open 每次新流、消费方负责关闭）+ `FileMedia/BytesMedia/FuncMedia/ReaderMedia`（有界复制）适配器；纯 Go 图片探测（PNG/JPEG 魔数 + `DecodeConfig`，声明 MIME 不一致/结构损坏/未知格式错误分类）；`Slide.AddPicture(ctx, src, PictureSpec)` + `PictureShape` 受控句柄（nodeStep 路径，AltText/IsDecorative 读写、`SetAltText/SetDecorative` 互斥语义 §8.1）；**四 Fit 模式**（FitOriginalSize 96dpi 换算 EMU / FitStretch 拉伸 / FitContain 等比缩小居中 / FitCover 经 `a:srcRect` 裁剪填充不重采样）；**媒体暂存与共享引用保护**：SHA-256 内容哈希去重限定安全图片类型（同页/跨页复用同一媒体 Part 与 rId），ReplaceImage 换图走"新增或去重复用 → 重定向本 pic rId → 旧关系条目无本页使用者才移除 → 全局关系归零才删旧媒体 Part"（保守口径不解析未知 Part，绝不破坏共享引用）；图片追加 spTree 末尾（顶层 z-order），cNvPr id/name 自动分配；缺省保留源媒体字节不隐式重采样 | media.go/picture.go/image_test.go（10 用例：四 Fit 几何与 srcRect 断言、alt/decorative 读写、同页+跨页去重、共享引用保护与孤儿清理、单消费替换、同内容 no-op、错误分类、closed、Save 往返、FileMedia 调用时固化）；**修复 commit 对会话新增 Part 删除语义（新增后同会话删除=直接丢弃，不写 deletedParts 防保存计划 ErrNotFound）**；**修复 relsXML 只读 pk 忽略 overrides/addedParts 致会话内新增关系流被当空流重编号（rId 冲突覆盖旧目标）**；根包覆盖率 73.1%→75.2%；vet/全仓测试/WASM(js+wasip1)/Linux 交叉构建通过 |
| 2026-09-08 | STYLE-01 基础占位符与样式解析（M2 第三项）：`TextRun.EffectiveFont(ctx ResolveContext)`（方案 §6.1 契约）逐属性返回 `ResolvedValue/ResolvedColor`（Value/Resolved/Fallback/Trace 三要素 + 来源链 StyleStep）；解析链 L1 run rPr → L2 pPr defRPr → L3 列表级别样式（占位符: layout 匹配占位符 lstStyle → master 匹配占位符 lstStyle → master txStyles[class]；非占位符: otherStyle）→ L4 主题缺省字体（title 类 majorFont，其余 minorFont）；**占位符匹配 (type,idx) 规范化全等（type 缺省 obj、idx 缺省 0，ECMA CT_Placeholder）不按坐标/名称**；主题沿真实关系图导航（slide→layout→master→theme / notes→notesMaster→theme，**新增 p.relsOf 读视图含已提交 rels 补丁**）；颜色保留原始 ColorSpec 同时输出 RGB（srgbClr 直出 / sysClr lastClr 优先 / clrMap bg1..tx2 间接映射 / lumMod lumOff shade tint 按序应用，phClr 与未知变换 STYLE_PARTIAL）；"+" 主题字体引用展开（+mj-*/+mn-*）；无法解析 STYLE_UNRESOLVED、仅 ctx.Fallback 采用并标 Fallback、ctx.Strict → ErrUnresolvedStyle | style.go/style_test.go（完整链 fixture：title/body idx1/body idx2/普通文本框 × lvl0/lvl2/run rPr 覆盖/notes 冒烟）；**修复 notesPartOf 改读视图致 createNotes 后同会话可再读备注**；根包覆盖率 57.3%→73.1%；vet/全仓测试/WASM(js/wasip1)/Linux 交叉构建通过 |

### M0 剩余任务（按实施计划 §6）

- [x] OPC-01 收口：真实 PPTX 样本冒烟（语料到位后）
- [x] XML-01 节点索引树：NodeRecord / 命名空间环境 / 未知子树保留 / 深度预算（MaxXMLDepth）
- [x] XML-02：文本/属性补丁与结构插入（补丁冲突检测、转义、区间降序）
- [ ] M0 垂直验证程序：含动画与未知扩展样本 → 只改一个 Run → 保存 → B1 哈希 + 同节点未知区字节不变断言（**最高优先，失败即回方案调整保存策略**；单元级雏形已就绪，待真实语料）
- [ ] QA-01：落实 ≥20 份语料样本与许可记录、首批修改前后金样入库

### 待确认事项（阻塞性）

1. ~~正式 module path~~（已定：`github.com/F31/go-pptx`，CORE-02）
2. ~~正式许可证~~（已定：Apache-2.0，与远程仓库一致，CORE-02）
3. 首批客户端 PowerPoint / WPS 的具体版本/平台（语料 README 待登记）
4. 团队人力（计划基线：2 开发 + 0.5–1 测试/语料）

## 工作包总表（34 项，来源：实施计划附录 A）

| WP | 名称 | 阶段 | 状态 |
|---|---|---|---|
| CORE-01 | 初始化 module、CI、许可、模板资源 | M0 | 进行中（首批已提交） |
| OPC-01 | ZIP 索引、PartName、资源预算 | M0 | 进行中（首批已提交） |
| XML-01 | NS 感知解析与原始跨度 | M0 | 已完成（代码+单测；样本冒烟待语料） |
| XML-02 | 文本/属性补丁与结构插入 | M0 | 已完成（代码+单测） |
| OPC-02 | 关系图、Content Types、主 Part 发现 | M1 | 已完成（代码+单测；真实样本冒烟待语料） |
| SAVE-01 | 保存计划、未变 Part 复制 | M1 | 已完成（代码+单测，B1 回归全绿；接 DocumentStore 后回填 BaseRevision） |
| SAVE-02 | 原子保存、失败恢复、输出检查 | M1 | 已完成（代码+单测，AT-11 等价用例全绿；平台真机测试随 CI 三 OS 扩展） |
| MODEL-01 | Presentation/Slide 句柄与惰性读取、事务/revision 骨架 | M1 | 已完成（代码+单测） |
| TEXT-01 | 段落/Run、属性 patch、备注 | M2 | 已完成（代码+单测；真实样本冒烟待语料） |
| TEXT-02 | 跨 Run 替换与整批变更 | M2 | 已完成（代码+单测） |
| STYLE-01 | 基础占位符与样式解析 | M2 | 已完成（代码+单测） |
| IMAGE-01 | PNG/JPEG 与媒体暂存 | M2 | 已完成（代码+单测） |
| GEOM-01 | 单位、组矩阵、四角边界 | M3 | 已完成（代码+单测；旋转正方向按 ECMA 语义，嵌套旋转翻转金样待语料） |
| TABLE-01 | 富文本表格、合并、样式子集 | M3 | 已完成（代码+单测；AT-09 通过；真实语料金样待 QA-01） |
| 格式深度子集 | 段落/Run/线条/主题样式矩阵引用链/颜色变换全集 | M3 | 已完成（代码+单测；起步解析 R 档；未知项→Unknown/诊断不臆造） |
| MEDIA-01 | 媒体输入、类型检查、probe | M4 | 已完成（代码+单测；播放真实客户端验证待 QA 冒烟） |
| AUDIO-01/02/03 | 配音/播放树/计时计划 | M4 | 已完成（代码+单测：AT-05/06/07/08/12 通过；真实客户端播放验证待 QA 冒烟） |
| CHART-01 | 受限图表与数据源适配 | M5 | 已完成（代码+单测：AT-10 代码级等价（缓存/工作簿一致）；真实客户端"可编辑"冒烟待语料） |
| CLONE-01 | 同文档受限复制 | M5 | 已完成（代码+单测：数据隔离金样与未知关系拒绝通过；跨文档复制属 M6） |
| CLONE-02 | 跨文档受限复制 | M8 | **已完成**（clonePlan 引入 srcDoc/crossDoc 分离；版式/母版按内容字节匹配复用，未命中整体 ErrUnsupportedEdit、零残留） |
| TOOL-01 | CLI 与 IR | M5 | 已完成（代码+单测：ir 包 schemaVersion 独立、cmd/pptx 六子命令统一退出码 0/1/2/3/4、只读约束与 --overwrite 开关、tracks.json schemaVersion 校验；交叉构建 WASM/Linux CGO=0） |
| GEOM-02/STYLE-02/LAYOUT-01/ANIM-02/VIDEO-01/CHART-02/TEXT-03 | M6 扩展七包 | M6 | **七包全部完成**（ANIM-02/VIDEO-01/TEXT-03/CHART-02/LAYOUT-01/STYLE-02/GEOM-02 按预排 ANIM-02→VIDEO-01→TEXT-03→CHART-02→LAYOUT-01→STYLE-02→GEOM-02 落地） |
| CAP-01/TIMIR-01/TPL-01/TOOL-02 | M7 创新 I | M7 | **四项全部完成**（CAP-01 能力 manifest + `pptx capability` CLI；TOOL-02 WASM 编译目标 + 浏览器端只读检查工具 + 离线静态网页；TIMIR-01 动画时序只读 IR；TPL-01 模板数据绑定引擎 + `pptx bind` CLI）（能力 manifest + `pptx capability` CLI + WASM 编译目标 + 浏览器端只读检查工具 + 离线静态网页 + node.js 烟雾测试 + 构建脚本 + 动画时序只读 IR）；余下 TPL-01 待启动 |
| DIFF-01 | M8 创新 II | M8 | **已完成**（语义 diff 与审计：ir.Diff 加权 LCS 页面对齐 + ShapeID 页内配对 + opaque diff 定位回溯；`pptx diff` CLI；NodePath 溯源接入 Shape 接口与 IR） |
| QA-01 | 语料、fuzz、兼容报告 | 持续 | 进行中（模板 + fuzz 八目标已落地：打开链路五目标 + 文本编辑两目标 + 模板绑定一目标；语料/金样/兼容报告待外部样本；**语料生成脚本 `scripts/gen_corpus/` 已起步**——opencode 处理中） |
