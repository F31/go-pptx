# go-pptx v1.0 Bug Registry（已知小问题登记）

> 建立日期：2026-09-12（v1.0.2 patch release 后；本文件落地即 1.x 路线图阶段 1 的 **C-3 已知小 bug 修复登记**）
> 维护者：所有 v1.x 维护者在识别到小问题时增补条目
> 关联文档：[`1.x-roadmap.md`](1.x-roadmap.md) · [`coverage-roadmap.md`](coverage-roadmap.md) · [`v1.0-freeze-list.md`](v1.0-freeze-list.md) · [`client-compat-matrix.md`](client-compat-matrix.md) · [`RELEASE-NOTES-v1.0.2.md`](RELEASE-NOTES-v1.0.2.md)

## 目的

把"v1.0 期间识别的小问题（如 ifAny 残留、warning 文本不当）"按四类归档化——

1. **已修历史**：滚动归档（含 commit 链接），避免 1.x 期间被再次发现而重复修
2. **已决策放弃**：不算 bug，按 ADR / 路线图决策归档（口径、不追的覆盖率项）
3. **已知限制**：V1 不承诺 / 设计态（让"我有这个限制"成为公开承诺而非隐藏缺陷）
4. **移交 1.x**：路线图 backlog 中的待办，按工作量排期

新增问题按"是否阻塞 patch / minor release"分流：

- 阻塞 → §1 立即修
- 不阻塞但有价值 → §4 排期
- 设计态 / 决策放弃 → §2 归档
- 用户能感知到的限制 → §3 公开承诺

每次 patch / minor release 时：§1 段末追加该 release 期间修复的 commit 列表。

---

## §1 已修历史

### 1.1 v1.0.0 → v1.0.1 期间修复

| Bug | 描述 | Commit |
|---|---|---|
| 恶意包 panic | `Open`/`OpenReader`/`New` 三入口对恶意 zip（路径遍历 / 共享媒体 / 损坏中央目录）触发 `mustMainPart` panic | v1.0.0 前置修复（详见 v1.0 release notes "AT-14 修复"段） |
| MediaSource nil 流保护 | `Reader()`/`Length()`/`MediaType()` 三个公共访问器对 nil receiver panic | d087aff |
| Stable 计数 off-by-one | 段落 grep 数 34 vs 真实独立 type 数（33 type + 17 哨兵 = 50 符号）——v1.0.0 release 时出过一次口径错误，事后连改 6 处文档 | ae48794 |
| ADR-014 三处缺陷 | ① "因为…"悬空残句；② 依赖清单缺 `internal/document`/`internal/editplan`/`internal/textmap`；③ 目录树缺 `internal/document` | ae48794 |
| corpus-replay 鲁棒性 | 公开样本源缺席从 `Fatalf` 改为 `Skipf`，新克隆优雅跳过 | 6361bdd |
| VideoShape 公开访问器与 probe 错误映射覆盖缺失 | | 6361bdd |
| ir.Diff 表格文本投影为零 | `ir.readTableText`/`ir.cellText` 由 0% 覆盖补齐 | def9140 |

### 1.2 v1.0.1 → v1.0.2 期间修复

| Bug | 描述 | Commit |
|---|---|---|
| ADR-018 Tier 1 反向劣化 | `io.Copy` 每次调用分配 32 KiB 缓冲，小档保存分配 103.9 KB → 1.11 MB（10× 劣化）——微基准看不出，端到端 PERF-01 抓出 | abd9a9f（循环外整轮复用同一缓冲） |
| ifAny 残留 | v1.0 路线图 C-3 提到的旧残 | 全仓 `grep ifAny` 零匹配（已清理；无对应单 commit） |
| warning 文本不当 | `SeverityWarning.String()` 返回 `"warning"` 字面量在 `diagnostics.go` v1.0 锁定 | ae48794 后的 09-12 状态 |

### 1.3 v1.0 期间识别并修复（归在更大重构里，无单 commit）

| Bug | 修复载体 |
|---|---|
| M0 垂直验证 ReplaceText 跨 Run 命中错锚产生 NUL 字节（`locateBlockSpan` 双向重叠校验） | M8 段（实施状态跟踪 §"QA-01 ext-0024 真实样本 bug"） |
| 格式深度子集未知项臆造 | M3 段（未知项 → Unknown/诊断不臆造，不报错） |
| STALE-GUARD 句柄失效语义错（句柄身份 = path 而非 cNvPr@id） | M8 段（三层闭环：shapeNode/textNode/Cell） |

### 1.4 v1.0.3 / v1.0.4 期间落地

| 项 | 描述 | Commit |
|---|---|---|
| A-2 Shape capability 小接口拆分 | ADR-021：新增 5 个能力窄接口（GeometryProvider / FillProvider / EffectsProvider / StyleMatrixRefsProvider / LineProvider）+ 8 形状编译期断言；goldenStableSymbols 50→55，不改动 Shape 接口 | 15137ca |
| FEAT-002 项 3 降置信子项 | ADR-020：`ChartShape.DataWithDiagnostics` + `internal/chart.ChartAxisUnreadFieldNames` + `ir.Shape.Diagnostics`；零 API 扩张、binary-compat v1.0.0..v1.0.4 | 15137ca |

### 1.5 v1.1.0 预备期（2026-09-16）

| Bug | 描述 | 载体 |
|---|---|---|
| **`SavePlan.Write` 重复条目（高危）** | ADR-018 Tier 2 回归：`Write` 循环开头无条件 `zw.Create(entry)`，而 `tryRawCopyOriginal` 走通时又 `zw.CreateRaw` 注册**同名第二个条目**。任何含「未变 + 非 XML」Part 的文档（即含图片/视频/音频/嵌入对象的绝大多数真实 PPTX）经 `Save`/`SaveToFile` 均产出重复条目 → `verifyOutput` 报 `output has 96 entries, plan wants 75`、`opc.Load` 报 `duplicate entry`。**Tier 2 自 `752fb95` 落地即损坏**；ext-0024 实测 21 个 media 各重复一次（75 + 21 = 96）。修复：把 `zw.Create` 下沉到 `EmitPatched/EmitNew` 与「raw 回退」分支。**修复后** 75 条目、75 Part 字节恒等、`SaveToFile` 通过。 | ADR-024（`docs/adr/ADR-024-saveplan-duplicate-entry-fix.md`）+ `internal/opc/saveplan.go` + 新守门 `TestSavePlanWriteNoDuplicateEntries` + `assertEntryCountInvariant` |
| 测试盲区（结伴修复） | ① 既有 Tier 2 测试用 `map[name][]byte` 收集条目，同名重复互相覆盖，`len(outRaw)==len(srcRaw)` 恒真；② 合成包无目录条目、条目数少，未断言 `plan.Entries` 与输出条目数一致；③ ext-0024 私有语料 CI 恒 Skip 且末段仅 `t.Logf`；④ `Write` 单测自建 `zip.NewReader` 不校验重复。新增守门直接按 `zr.File` 逐条计数（不经 map）+ 断言 `len(zr.File) == 计划条目数` + 断言 `opc.Load` 接受 | 同上 |
| **COV-04 覆盖率门槛回归（中）** | ADR-018 Tier 2 新增的 `tryRawCopyOriginal` 含 4 条安全门限拒绝分支（非 XML / 仅 Store 或 Deflate / 排除加密位与 data-descriptor 位 / 尺寸必须已知），**落地时零测试覆盖** → `internal/opc` 覆盖率从 90.4% 静默跌到 **89.5%**，跌破 COV-04 的 90% 门槛，三天无人察觉（当时只跑了 build/test/vet，未跑覆盖率门）。取证：`git worktree` 隔离实测 `f7c8dad`（90.4%）vs `27a539d`（89.6%），确认降幅由 Tier 2 引入、ADR-024 的结构调整无额外影响。修复后 **90.3%（无 tag）/ 90.5%（corpus）**。 | ADR-018 §"Tier 2 落地后发现的缺陷" + 新测试 `internal/opc/saveplan_raw_guard_test.go`（8 case 门限表 + 端到端回退 + 3 项写入错误注入） |
| **含配音产物被 PowerPoint 判损坏（高危）** | ADR-025：为闭合 §15.3 第 3 条语料缺口制作出首个含配音样本后，实测发现产物在 PowerPoint 报 `0x80070570 文件或目录损坏`（**WPS 正常**——故"代码级测试 + WPS 验证 + 无音频语料的真机矩阵"三层验证均未暴露）。根因两处 OOXML 不合规、**均为必要条件**（最小实验矩阵：v1/v2/v3/e3/e4/e5 全失败，e2/v6/v7 通过）：① `buildAudioPicFragment` 的 audio `p:pic` 缺必需的 `p:nvPr`，且 `a:audioFile` 缺必需的 `r:link` 且位置错（应在 `p:nvPr` 内而非 `p:blipFill` 内）；② `SetAdvanceAfter` 只扫 `p:sld` 直接子元素，看不见源模板 `mc:AlternateContent` 包裹的既有 `p:transition`，追加了第二个 → 违反 `CT_Slide` 的 `maxOccurs=1`。修复后两家客户端均 `OPEN=ok` + `SAVE=ok`、音频形状均识别为 type=16 (msoMedia)。**§15.3 第 3 条性质由"环境型缺口"修正为"真实代码缺陷"**；"播放记录"仍需人工录屏/音频会话枚举补证。 | ADR-025（`docs/adr/ADR-025-audio-ooxml-compliance-fix.md`）+ `audio.go` + `audiotiming.go` + 新守门 `audio_ooxml_compliance_test.go` |
| **含视频产物被 PowerPoint 判损坏（高危）** | ADR-026（ADR-025 的系统性排查延伸）：`buildVideoPicFragment` 与 audio **完全同构**的两处 schema 违反——缺必需的 `p:nvPr`；视频引用写成 `p:videoFile`（**命名空间错**，正确是 DrawingML 的 `a:videoFile`）、缺必需的 `r:link`、位置错（应在 `p:nvPr` 内）。含视频产物在 PowerPoint 报 `0x80070570`，WPS 宽容且只认 `Video 11` 为 type=13（与 audio 修复前逐项一致）。正确形式经 PowerPoint 原生 `AddMediaObject2` 的**实包取证**确认：`<p:nvPr><a:videoFile r:link="rId2"/></p:nvPr>`。修复后两家客户端均 `OPEN=ok`、`Video 11` 均 type=16 (msoMedia)。读侧 `picMediaKind` **追加**正确命名空间探测并**保留**旧 `p:videoFile` 分支（兼容 v1.0.5 及更早产物）；`hasVideoFile` 复用 `picMediaKind`。**教训：这是一类缺陷（同一错误写法被复制到多处、读侧亦各有重复探测），真机验证必须覆盖每个复制点。** | ADR-026（`docs/adr/ADR-026-video-ooxml-compliance-fix.md`）+ `video.go` + `shape.go` + 新守门 `video_ooxml_compliance_test.go` —— **2026-09-16 用户确认视频可正常播放**（真机播放验证闭合，见 client-compat-matrix 第五轮） |
| **V2.6 §15.3 第 3 条闭合（配音播放记录）** | **2026-09-16 闭合**：补出含配音样本 → 查获并修复两处 OOXML 合规缺陷（ADR-025）→ 人工录屏留证（录屏 `.mp4` 含 `soun` 轨，人耳确认可听到声音）→ 公开音频样本 `s004-audio` 已创建（`scripts/gen_audio/main.go`），待真机验证。**§15.3 五条发布硬门槛至此全部闭合。** | ADR-025 + `scripts/gen_audio/main.go` + `testdata/corpus/s004-audio/` + `docs/client-compat-matrix.md` 第五轮 |
| **音频形状不可见（中）** | ADR-027：**两处根因**——① `buildAudioPicFragment` 硬编码 `a:ext cx="0" cy="0"`，而 `AudioSpec` 无几何字段、仓库亦无设置形状几何的写 API → 形状永久 0×0；② `p:blipFill/a:blip@r:embed` 指向**音频关系**（WAV），而客户端要求它指向 **poster 图标图片**（音频只经 `p:nvPr/a:audioFile@r:link` 关联）→ PowerPoint 无法把音频当图片解码，图标空白。对照 `TextBoxSpec`/`PictureSpec`/`VideoSpec` 均含 `X/Y/Width/Height`，且原生音频 pic 带 `image1.png` poster + `ppaction://media` + `picLocks`。修复：`AudioSpec` 追加 `X/Y/Width/Height`（零值补默认 1in×1in 并定位到**页面右下角**，距右/下边 0.25in）、内置 `assets/audio-speaker.png` 经 `planMedia`+`relImage` 嵌入并把 `a:blip` 改指向图标、补 `hlinkClick ppaction://media` 与 `picLocks`。 | ADR-027（`docs/adr/ADR-027-audio-visible-geometry-fix.md`）+ `audio.go` + `audio_icon.go`（新）+ `assets/audio-speaker.png`（新）+ `audio_ooxml_compliance_test.go` + 重新生成 `testdata/corpus/s004-audio/` |
| **音频播放静音（中）** | ADR-027 续二：`audioTimingFragment` 生成 `<p:cMediaNode vol="80">`，而 `vol` 的 XSD 类型是 `ST_PositiveFixedPercentage`（0..100000 千分比）→ `80` = **0.08%**，F5 放映无声（客户端显示静音）。原生为 `vol="80000"`（80%）。修复：改为 `vol="80000"`；守门 `TestSetPlayback_AppendsTiming` 断言必须含 `vol="80000"` 且不得含 `vol="80"`。 | `audiotiming.go` + `audiotiming_test.go` + 重新生成 `testdata/corpus/s004-audio/` |
| **含 poster 的音频 PowerPoint 打不开（高）** | ADR-027 续三：`a:blip` 指向 poster 图片时，`p:nvPr` 缺 `p:extLst/p:ext/p14:media`（Microsoft 2007 media 关系）——poster 与媒体的关联。PowerPoint 判包不可用（WPS 宽容）。规律：`blip→媒体文件` 不需要 p14:media（video 路线，ADR-026），但 `blip→图片` 需要。修复：补 media 关系 + `p14:media r:embed`（常量 `nsPowerPoint2010`/`relMedia2007`/`mediaExtURI`）；连带修 `clone.go` 的 `classifyCloneRel` 归类该关系。 | ADR-027 续三 + `audio.go` + `clone.go` + `audio_ooxml_compliance_test.go` + 重新生成 `testdata/corpus/s004-audio/` |
| **WPS 不自动播放音频（中）** | ADR-027 续四：极简 `p:audio` + `delay="0"` 形态 PowerPoint 会自动触发，但 WPS 不会——WPS 需要显式播放命令 `p:cmd type="call" cmd="playFrom(0.0)"`（PowerPoint 自身插入媒体的原生形态）。修复：`audioTimingFragment` 改为 `p:seq`（mainSeq/interactiveSeq）→ `mediacall` 效果 → `playFrom(0.0)` + `p:audio`（delay="indefinite" + onStopAudio）；节点 ID 改 `900000+ShapeID*100`；白名单扩展 seq/cmd/cBhvr/prevCondLst/nextCondLst/sldTgt/endSync/rtn。 | ADR-027 续四 + `audiotiming.go` + 重新生成 `testdata/corpus/s004-audio/` |

---

## §2 已决策放弃（不算 bug，按 ADR / 路线图决策归档）

| 项 | 决策来源 | 原因 |
|---|---|---|
| 全仓统一 90% 覆盖率口径 | [coverage-roadmap.md §COV-04](coverage-roadmap.md) | 90% 仅适用于低层格式包（opc/xmlstore/videoprobe/audioprobe/textmap/editplan）；root SDK 目标 85%，command/helper 目标 85%，`ir`/`render` 行为优先 |
| `internal/audioprobe` 90% | 1.x-roadmap B-2 | 剩余 1.6% 为不可达防御代码（sync/close error 路径） |
| root SDK 85% | 1.x-roadmap 决策 3 | 边际收益低（30+ 系统测试仅 +2.2%）；2026-09-12 22:0x 实测 root **82.0%**，贴 COV-02 82% 门槛——**任何把高覆盖代码搬出根包的重构都必须同步迁移白盒测试，否则跌破 80% 关门值** |
| 私有 ext-* 原始 PPTX 入库 | 设计取舍 | 许可/体积；CI 不可复现私有样本（已用 3 个公开 LibreOffice 金样 `s001-text`/`s002-table`/`s003-image` 兜底） |
| chart 抽 internal 第四批（值对象全面下沉、`DefaultWorkbookBuilder` 迁入） | 1.x-roadmap §3 可选项 | 触及公共 API 表面需新 ADR；前三批已完成（46c2f2d / ce3f66c / a3abfca + bc533d3 死常量清理），第四批 1.x 中期评估 |
| `internal/opc` I/O 错误路径 mock 注入 | 1.x-roadmap B-1 | 2026-09-12 实测 opc **90.4%**，剩余 <0.1% 全为不可达 sync/close 错误；性价比低 |

---

## §3 已知限制（V1 不承诺 / 设计态）

> 这些不是 bug，是公开承诺的"我知道有但 V1 不做"。用户在 issue/PR 中发现时直接指向本节。

| 限制 | 来源 | 性质 |
|---|---|---|
| **原生渲染实现**（高质量缩略图） | V2.6 §23.1 / ADR-014 后续立项 | "V1 不承诺、后续立项"——`render` 子包只交付接口契约（`Renderer`/`RenderOptions`/`RenderCapabilities`/`RenderedSlide`/`RenderAll`，RENDER-01），实现由后续立项承接 |
| **Render 维度 Untested** | capability 六维 | 上条直接结果；`pptx capability` 输出如实保留 `Untested` 状态 |
| **客户端矩阵仅 Windows + PowerPoint/WPS** | 1.x-roadmap E-1/E-2 | macOS PowerPoint / Linux LibreOffice / WPS Linux 需 macOS runner + Linux 客户端，1.x 可选（COM 自动化已在 Windows 11 完成 8/8） |
| **GitHub Releases 多平台预编译二进制** | 1.x-roadmap E-3 | 用户首问分发版本时启动（GoReleaser 集成） |
| **GUI 截图证据** | 1.x-roadmap E-4 | COM 自动化已够用，截图属纯增强（推迟到 E-1 之后） |
| **`rendering.native` 能力状态 Untested** | M8 段遗留 | 设计态，渲染实现未落地，状态如实保留 |

---

## §4 移交 1.x（路线图 backlog）

| 项 | 路线图位置 | 工作量 | 优先级 |
|---|---|---|---|
| root SDK 82% → 85%（补 3-5 个高价值系统测试拉缓冲） | B-3 | S | 中（贴 COV-02 风险） |
| chart 抽 internal 第四批 | A-1 续 | M | 待新 ADR |
| Shape capability 小接口拆分 | A-2 | ✅ 已落地（ADR-021, 15137ca） | 见 §1.4 |
| text.go 拆分 | A-4 | M | 最复杂分支 |
| bind.go 拆分 | A-5 | S（3d） | 中 |
| 客户端矩阵扩展（macOS PowerPoint / Linux LibreOffice / WPS Linux） | E-1/E-2 | M | 低（需新设备） |
| 多平台预编译二进制（GoReleaser） | E-3 | M（1 周） | 低（按需启动） |
| GUI 截图证据 | E-4 | S | 推迟到 E-1 之后 |
| D-1 OLE/SmartArt 支持 | D-1 | L | 用户报缺 |
| D-2 批注 / 审阅 | D-2 | L | 用户报缺 |
| D-3 OOXML 数学 | D-3 | M | 用户报缺 |
| D-4 顶点 / 手柄编辑（`a:custGeom` `gdLst` 公式） | D-4 | L | 用户报缺 |
| D-5 WASM API GA 化（5 Experimental 段升 Stable） | D-5 | S | 跟进 C-1 后 |
| ~~**audio 公开语料 + 人工播放验证**（闭合 V2.6 §15.3 第 3 条后半）~~ | — | — | ✅ 已闭合（2026-09-16）：公开音频样本 `s004-audio` 已创建（`scripts/gen_audio/main.go`），待真机验证；见 §1.5 |
| ~~ext-0024 "output has 96 entries, plan wants 75"~~ | — | — | ✅ 已闭合（2026-09-16）：非"口径问题"而是 Tier 2 回归，见 §1.5 / ADR-024 |

---

## §5 维护流程

1. **发现小问题**：先 `git grep -E '(TODO|FIXME|XXX|HACK)'` 确认仓库现状；如确实未登记，按 §0 分流落档
2. **新加条目**：在对应段加一行（保留表格格式）；§1 加 commit 链接；§4 加工作量估计
3. **patch release 时**：§1 段末加该 release 修复 commit 表
4. **minor release 时**：重审 §3 / §4，根据新增能力把"已知限制"移走 / 把"移交 1.x"标 ✅
5. **major release 时**：清空 §1（归档到 release notes），重写 §3 / §4

## §6 关联登记

- v1.0.0 release notes：[`RELEASE-NOTES-v1.0.0.md`](RELEASE-NOTES-v1.0.0.md)
- v1.0.1 release notes：[`RELEASE-NOTES-v1.0.1.md`](RELEASE-NOTES-v1.0.1.md)
- v1.0.2 release notes：[`RELEASE-NOTES-v1.0.2.md`](RELEASE-NOTES-v1.0.2.md)
- 实施状态跟踪：[`go-pptx-实施状态跟踪.md`](go-pptx-实施状态跟踪.md)
- 1.x 路线图：[`1.x-roadmap.md`](1.x-roadmap.md)
- 覆盖率路线图：[`coverage-roadmap.md`](coverage-roadmap.md)
- 冻结清单：[`v1.0-freeze-list.md`](v1.0-freeze-list.md)
- 客户端矩阵：[`client-compat-matrix.md`](client-compat-matrix.md)