# 2026-09-22 工作日志

## 22:0x ppts《go-pptx 特性与bug跟踪计划》V1.0 §7 应答

**背景**：ppts 开发提交《go-pptx 特性与bug跟踪计划》V1.0 §7（FEAT-001/002/003 + BUG-001）。我先做"代码对照分析"——核对当前 HEAD c47b7a3 真实状态——再按候选清单选 FEAT-003 读侧补全 + BUG-001 自愈同步实施。

**调研结论**：

- **BUG-001 已自愈**：ADR-017 r3（commit a3abfca）+ 后续 33 个 commit 已删除 chart.go 旧 `validateChartData`；当前仅 chartfrag.go:22 一处声明。
- **FEAT-001** 真机门禁非 go-pptx 代码工作，归 ppts 端 PowerPoint 安装测试。
- **FEAT-002** 项 1（备注过滤）已隐式可用（`notesPhType` 严格只匹配 type=body），仅需文档注记。项 2（阅读顺序）+ 项 3（图表单位）需新 ADR。
- **FEAT-003** 项 1（隐藏页报告）+ 项 2（advTm 读侧）真缺口——`Slide.Hidden()` 与 `Slide.AdvanceAfter()` 公开方法完全缺失；IR `Page.Hidden` 字段不存在。
- 页序（FEAT-003 项 1.1 + 项 3）已**实质可用**：`Slides()` 严格按 presentation.xml `p:sldIdLst` 顺序，零改动。

**做了什么（v1.0.3 patch）**：

1. **新增 Stable 公开方法 `Slide.Hidden() (bool, error)`**（`slide.go:118-149`）—— 读 `p:sldId@show="0"`；binary-compat。
2. **新增 Stable 公开方法 `Slide.AdvanceAfter() (time.Duration, bool, error)`**（`slide.go:151-216`）—— 读 `p:transition@advTm`，与 `SetAdvanceAfter` 对偶；binary-compat。
3. **新增 IR 字段 `Page.Hidden *bool`**（`ir/ir.go:97-103`）+ **Options.IncludeHidden bool**（默认 true）（`ir/ir.go:140-156`）—— Experimental 档（JSON 协议稳定性需 ADR）。
4. **补 `notes.go` 顶部"隐式过滤契约"注记**（`notes.go:18-31`）——文档化 `SpeakerNotesText` 自动过滤 hdr/ftr/sldNum/dt 模板占位符。
5. **同步守门**：`api_surface_test.go` `wantStableMethods: 127 → 129`，`goldenStableMethods` 加 `Slide.AdvanceAfter` + `Slide.Hidden`。

**测试成果**：6 个新增 slide_test.go + 3 个新增 ir_test.go 断言，9/9 PASS。整套 14/14 包 ok + corpus tag 14/14 包 ok + 4 目标交叉构建 PASS。

**文档同步**：
- CHANGELOG.md 新增 `[1.0.3]` 段
- 新建 `docs/ppts-sync/` 目录（3 文件 + README）：
  - `README.md`：ppts 跟踪计划索引 + 可直接复制的同步文本
  - `BUG-001-self-healed.md`：自愈证据 + 提交链 + 质量门禁实测
  - `FEAT-002-notes-filtering-contract.md`：项 1 文档化记录（无代码改动）
  - `FEAT-003-hidden-advtm-read.md`：读侧补全实施详细记录

**兼容性 v1.0.3 = binary-compat with v1.0.0/v1.0.1/v1.0.2**：
- Stable 50 符号不变；Stable 方法 127→129（+2）
- Experimental 5 段不变
- 公共 type 总数 158 不变
- 17 哨兵语义锁死不变
- v1.0.3 tag 待 ppts 端 VERIFIED 后补打

**给读者的访问入口**：
- go-pptx 仓库：[`docs/ppts-sync/`](https://github.com/F31/go-pptx/tree/main/docs/ppts-sync/) 目录（README 含可复制同步文本）
- ppts 项目工程：从 ppts 端 `internal/project/reader_test.go` + `internal/integrations/narrationwriter_test.go` 增加 Slide.Hidden() / Slide.AdvanceAfter() 断言
- 测试覆盖：6 个新 TestSlide_* + TestOptions_Defaults 增 IncludeHidden + TestFromPresentation_PageHiddenProjection 2 子测

## 22:0x v1.0.3 patch 发布流程（参照 v1.0.2 模板）

**背景**：v1.0.3 实施 commit `945dc37` 已 push origin，但 tag 未打。按 v1.0.2 已沉淀的发布流程模板跑发布前 1-3 步 + 用户授权后打 tag + push。

**做了什么**：

1. 写 `docs/RELEASE-NOTES-v1.0.3.md`（参照 v1.0.2 模板，9 段）—— 关键不变量表（Stable 方法 127→129）/ Added（FEAT-003 读侧补全，含 `Slide.Hidden` + `Slide.AdvanceAfter` + `ir.Page.Hidden` + `ir.Options.IncludeHidden` 四条详细契约）/ Documented（FEAT-002 项 1 + ppts-sync 4 文件）/ Fixed（BUG-001 自愈登记）/ Verification / Compatibility / 反馈 / Acknowledgments。
2. 编辑 `.workbuddy/memory/MEMORY.md` v1.0.0/1/2 段后追加 v1.0.3 段（HEAD=945dc37，binary-compat，Stable 方法 127→129）。
3. 追加日志到本文件。

**兼容性总账**（v1.0.3 vs 之前）：

| 项 | v1.0.0 | v1.0.1 | v1.0.2 | v1.0.3 |
|---|---:|---:|---:|---:|
| Stable 段落 | 34 | 34 | 34 | 34 |
| Stable 符号 | 50 | 50 | 50 | 50 |
| Stable 方法 | 127 | 127 | 127 | **129 (+2)** |
| Experimental 段落 | 5 | 5 | 5 | 5 |
| 公共 type 总数 | 158 | 158 | 158 | 158 |
| 错误哨兵 | 17 | 17 | 17 | 17 |

**给读者的访问入口**：
- v1.0.3 release notes：`docs/RELEASE-NOTES-v1.0.3.md`
- ppts 跟踪计划应答索引：`docs/ppts-sync/README.md`
- 兼容性全自动守门：`api_surface_test.go`（wantStableMethods 129 由本次+2）

**完整发布流程模板（v1.0.3 第 3 次跑通）**：① 实施 commit push origin → ② 写 `docs/RELEASE-NOTES-v1.0.x.md` 参照上一版 → ③ 编辑 MEMORY.md v1.0.x 段（binary-compat + 量化 Stable 变化）→ ④ 追加 `.workbuddy/memory/{date}.md` 日志段 → ⑤ commit + push → ⑥ `git tag -s v1.0.x -m "..."` → ⑦ `git push origin v1.0.x` → ⑧ 再 commit MEMORY.md + log sync → ⑨ push。本次 v1.0.3 与 v1.0.0/1/2 一致同款。

**当前状态（待 tag 步骤）**：HEAD=945dc37 已 push origin/main 同步；RELEASE-NOTES-v1.0.3.md 已写；MEMORY.md + 2026-09-22.md 待 commit。

**遗留待办（用户授权后才执行）**：
- git tag -s v1.0.3（SSH 签名，仓库级 `gpg.format=ssh` + `user.signingkey=~/.ssh/id_ed25519`，v1.0.0/1/2 同路径成功）→ git push origin v1.0.3 —— 不可逆发布动作，留用户审阅
- 远端 CI 全绿确认（含 ubuntu race job，本机无 gcc 跑不了）
- ppts 端 FEAT-003 接收 + BUG-001 标 CLOSED（自愈）
