# 2026-09-09

## CHART-01（M5 首项）：受限三类图表，代码+单测完成

- 新增 `chart.go`：`Slide.AddChart`（柱/折/饼规范布局 chartSpace + 嵌入工作簿 + chart→package 与 slide→chart 关系 + spTree 末尾 graphicFrame）、`ChartShape.Data()`（strRef/numRef/strLit/numLit 四路缓存读取，组合图取首个受限组）、`ChartShape.SetData`（受限更新：`chartIsCanonical` 结构白名单校验，外来/被改写图表 ErrUnsupportedEdit 整体拒绝；通过后 chart Part 与工作簿同一数据快照重建，AT-10 缓存/工作簿一致）。
- 新增 `chartbook.go`：`ChartWorkbookBuilder` 可替换适配接口 + `DefaultWorkbookBuilder`（纯标准库最小 xlsx，inlineStr，确定性输出；图表引用 Sheet1!$A$2:$A$n / $B$2:$B$n 与工作簿布局配套约定）。
- `shapes.go`：ShapeChart 分类；`presentation.go`：chartWorkbookBuilder 字段。
- chart_test.go 9 用例全绿；覆盖率 75.4%→75.7%；vet/gofmt/CGO=0 Linux/js+wasm/wasip1+wasm 通过。
- 踩坑：**同一条消息里对同一文件的多个 Edit 并行执行会相互覆盖**（各自基于同一原始内容写回，只留最后一个）——对同一文件的多处修改必须分多轮串行 Edit。
- 下一步：M5 剩余 CLONE-01（同文档受限复制，依赖 CHART-01）与 TOOL-01（CLI 与 IR）。

## CLONE-01（M5 第二项）：同文档受限页面复制，代码+单测完成

- 新增 `clone.go`：`Slide.Clone(ClonePolicy)`。两阶段：buildClonePlan 纯读取验证（拒绝全部发生在暂存前）+ applyClonePlan 单事务提交（snapshotPending 快照失败恢复 pending——首个带显式回滚的公共修改方法）。
- 核心策略：容器（slide/notes/chart）递归闭包遍历，remap 入口登记防循环；版式/母版复用；图表+工作簿**总是独立克隆**（数据隔离硬性要求）；媒体默认共享、IndependentMedia 可独立；外部关系逐字节保留；未知内部关系类型 ErrUnsupportedEdit 整体拒绝（AT-13 同文档口径）。
- 引用重写化简：克隆容器的关系流是全新 Part → 逐条保留源 rId/Type/TargetMode/转义原文，仅对命中 remap 的内部关系用 `SetAttrValuePatch` 重写 Target（同目录前缀 retargetRel）；slide XML 字节级复制（rId/ShapeID/timing 节点 ID 均页内作用域，恒等成立）。notesSlide 回引校验必须指向源页。
- 名字分配 `allocCloneName`：数字尾名（chart1.xml）走 nextPartSeq + 事务内 allocSeq/allocUsed 去重（nextPartSeq 只看已提交视图，同事务多次同前缀会撞名）；非数字尾名 stem-N。
- AudioProfile 克隆：派生 TrackKey（-c/-c-N 唯一化）+ SlidePart→目标页 + 独立媒体时 MediaPart 重映射，`stageAppendAudioProfiles` 同事务追加（不单独 commit，区别于 recordAudioProfile）。
- clone_test.go 9 用例：图表数据隔离金样（SetData 改克隆，源 chart+workbook 字节不变）、未知关系拒绝零残留、共享/独立媒体、notes 回引重写、TrackKey 两次克隆不冲突、保存往返、外部超链接逐字节保留。覆盖率 75.7% 持平；交叉构建全过。
- 下一步：M5 剩余 TOOL-01（CLI 与 IR）。
