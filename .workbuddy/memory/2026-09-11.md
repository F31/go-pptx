# 2026-09-11

## 凌晨段（v1.0.0 tag 发布 + 收尾，commit 5d61f97）

**v1.0.0 正式发布**：`git tag -s v1.0.0`（SSH 签名）→ tag 对象 92dc8622 → commit 44b9ba7，
推送 origin 成功（2026-09-11 06:00）。

- 前一晚 commit 44b9ba7（发布通告草稿）首次推送输出为空（沙箱静默失败），本轮重试
  `19efa3f..44b9ba7` 成功。
- 签名前置检查：本机无 GPG secret key（gpg keyboxd 报 Configuration error）→ 采用 SSH 签名：
  仓库级 `git config gpg.format ssh` + `git config user.signingkey ~/.ssh/id_ed25519`（Git 2.55
  支持；`~/.ssh/id_ed25519` 已绑定 GitHub 账号 jinfeng105，GitHub 可验证显示 Verified）。
- tag message 按 freeze list §E 第 11 项写 API stability 摘要（51/5/102/0 + M0-M8 + 跨平台 +
  已知限制 + Refs），用 `-F .commit-msg-tag.txt` 传入。
- `git cat-file tag v1.0.0` 验证含 `-----BEGIN SSH SIGNATURE-----` 块；`git ls-remote --tags
  origin` 确认 `refs/tags/v1.0.0` → 92dc8622、`^{}` → 44b9ba7。
- 本地 `git tag -v` 需 `gpg.ssh.allowedSignersFile` 才能验证 SSH 签名（本地未配，GitHub 侧验证）。

**收尾文档（commit 5d61f97，4 files）**：
- `docs/v1.0-freeze-list.md`：状态"评审中"→"已冻结"；§"v1.0 评审执行进度"新增
  "2026-09-11 — T-3 日补齐 + T-0 发布"段（最终数字 51/5/0/102/158 + 发布级缺口 1.x 收敛）
- `docs/go-pptx-实施状态跟踪.md`：v1.0 tag 流程 checklist 5/5 全勾选 + "v1.0.0 已发布"引注
- `CHANGELOG.md`：v1.0.0 日期 2026-09-10 → 2026-09-11（tag 实际时间）
- `docs/RELEASE-NOTES-v1.0.0.md`：头部状态"T-0 草稿"→"已随 git tag -s v1.0.0（SSH 签名）发布"

**踩坑（第二次出现）**：`git commit -F .commit-msg-*.txt` 报
`fatal: could not read log file ... No such file or directory`（exit 128，`&&` 链中断导致
push 未执行），但 **commit 实际创建成功**（HEAD 已更新）——Windows Git Bash 与 Write 工具
创建的 `.` 开头隐藏文件之间存在竞态。处理：fatal 后先 `git log` 核实 commit 是否已创建，
已创建则直接 push，不要重复 commit（否则产生空 commit 或重复提交）。

**v1.0 发布全链路收口**（T-2 周启动到 T-0 tag，全部 2026-09-10 ~ 09-11）：
- 评审 5 阶段 + T-3 日补齐：51 Stable 段落（34 独立 type + 17 哨兵聚合段 1）/ 5 Experimental /
  0 Deprecated / 102 API / 158 总
- CORPUS-01 工程化入库闭环（CI + helper + 指南）
- CHANGELOG + 冻结通告 + RELEASE-NOTES 三份发布文档
- v1.0.0 tag（SSH 签名）+ 收尾登记

**剩余（1.x 期间收敛，非 v1.0 阻塞）**：
- L3 客户端矩阵（PowerPoint/WPS 真机冒烟）—— 外部依赖，待真机接入
- 覆盖率 86.6% → 90% —— 边际成本高，非硬性门槛（ADR-015 §4）
- 新增导出符号按 freeze list §D 评审 checklist 走"待评审"区

## 上午段（v1.0 技术白皮书，commit 0218b29）

**新增 `docs/go-pptx-技术白皮书.md`**（746 行 / 12 章 / 50+ 子章节）：
- 1 产品概述 / 2 设计哲学与产品定位 / 3 技术架构 / 4 功能特性 /
  5 API 稳定性分级体系 / 6 质量保障与工程纪律 / 7 工具链与命令行 /
  8 产品特色与差异化优势 / 9 技术创新点（7 项）/ 10 典型应用场景（6 类）/
  11 已知边界与 1.x 路线图 / 12 版本、许可与社区

**创新点 7 项（§9）**——业内少有 / 首创的工程实践：
- STALE-GUARD 句柄身份三层闭环（cNvPr@id 复用 OOXML 稳定身份）
- xmlstore 命名空间感知 span patch 引擎（垂直验证 ext-0024 差异区间 1 字节）
- DIFF-01 加权 LCS + Jaccard 双相似度页面对齐（`go-pptx.diff/1.0`）
- Capability Manifest 六维能力披露（Inspect/Create/Edit/Preserve/Render/Play）
- 三级 API 稳定性 + 升降档规则（ADR-015 godoc 段落 + 评审流程）
- 公开金样 CI 闭环（CORPUS-01：3 公开 + 33 私有索引 + 垂直验证）
- 原子保存 + 并发修订快照（ErrOutputExists + ErrConcurrentModification）

**差异化优势（§8）**：服务端原生 + 4 平台 CI、字节级保真受控编辑、三级 API 承诺、
真实语料 + 性能 + fuzz 三位一体、编辑器到浏览器全链路闭环。

**信息源交叉验证**：与 v1.0-freeze-list.md（51/5/0/102/158）、CHANGELOG.md、ADR-014/015/016、
PERF-01-benchmark-report.md、M8 总结数字全部一致；5 个非锚点 markdown 链接全部验证存在。

**纯文档工作**：不影响 Go 代码、不影响 CI 验收。
- commit `0218b29 docs: go-pptx 技术白皮书（v1.0 综合性技术披露）`
- 推送 origin/main 成功：5d61f97..0218b29，HEAD 一致

**踩坑**：git status 在中文文件名输出八进制转义 `\346\212\200\346\234\257\347\231\275\347\232\256\344\271\246`
（CRLF 警告），不影响 add/commit/push，仅显示层 UTF-8 转义。