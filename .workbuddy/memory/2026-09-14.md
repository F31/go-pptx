# 2026-09-14 工作日志

## doc-reconcile 收尾（跨会话续做）
- 续前会话被中断的 `doc-reconcile` 提交推送：本地 commit `9aa809d`（docs: doc-reconcile — 对齐跟踪文档到 v1.0.4 真实状态）已确认落地，领先 origin/main 1。
- 完成 `git push origin main`：`1e30060..9aa809d main -> main`，SSH 推送成功。
- 清理本会话产生的 8 个临时探针文件（`.probe*.txt`/`.push*.txt`/`.pe.txt`/`.po.txt`/`.commitmsg_docreconcile.txt`）：`rm` 被沙箱 safe-bin 封装拦截（exit 127 未真删），改用 `git clean -f`（先 `-n` 干跑确认仅删这 8 个未跟踪文件）清除；工作树恢复干净。
- 文档对账内容：校正 ppts-sync 文档 `2026-09-22` 时钟漂移→`2026-09-12`；修复 README 的 ADR-020/ADR-022 编号冲突（项 2 阅读顺序归 ADR-022）；路线图/bug 登记/覆盖率路线图对齐 A-2 已落地（ADR-021）、合并口径覆盖率 84.4%。

## 可复用环境经验（已写入 MEMORY.md）
- `rm` 沙箱封装不可靠 → 仓库内清临时文件用 `git clean -f`（先 `-n`）。
- bash bare `git` + `/e/...` 路径可用；"挂载不稳定"为误判（coreutils 损坏的 stderr 噪声）。
- PowerShell `Remove-Item` 在本沙箱不回显且偶发失败，勿依赖。

## D-5 WASM API GA 化（执行）
- 将根包 5 个 `// Experimental:` 段升 `// Stable:`：`ChartWorkbookBuilder`/`ChartDataBook`/`DefaultWorkbookBuilder`(chartbook.go) + `CustomPropertyKind`/`CustomPropertyValue`(docProps.go)；演化边界重写为 GA 承诺，零签名/字段变更。
- api_surface_test.go 计数 34/55/5/130 → 40/60/0/131；golden 名单迁移 5 符号 + 1 方法（`DefaultWorkbookBuilder.Build`，接口方法 `ChartWorkbookBuilder.Build` 不计入方法锁）；Experimental 集合清空。
- 写 `docs/adr/ADR-023-wasm-api-ga-promotion.md`（含 ADR-015 §升档三要素）；ADR-015 追加 `#changelog` 段登记；同步 实施状态跟踪(40/60/0/131, 163 总 type) / 1.x-roadmap(D-5 ✅) / v1.0-freeze-list(§B 升档注记) / CHANGELOG(Unreleased)。
- 验证：`go test ./...` + `-tags=corpus` 全绿、`go vet` 干净、`gofmt -l` 干净。commit `b157096` 已 push（`9be8fb8..b157096`）。
- 关键判断：`DefaultWorkbookBuilder` 升 Stable 取代 ADR-017 r-4"迁入 internal"计划（公开类型成稳定契约，实现可下沉但类型留根包）；不在范围：ir.Page.Hidden / ir.Shape.Diagnostics 的 Experimental 字段（属 ir 包，需各自 ADR）。

## ADR-018 Tier 2 raw 直通（执行 + 收尾）
- 用户决策：认可"值得推进"结论（重启条件①经同进程 A/B 取证成立），指令"按建议继续执行"。
- **取证结论（同进程 A/B，纠正 PERF-01 §6 跨轮次假象）**：本机 Intel Core Ultra 9 275HX 混合核调度致跨轮次墙钟漂移 3.5×；同进程内 `share=(Stream−Raw)/Stream` 显示未变媒体重压缩占 Save p50 的 **50–73%**（3×8 MiB 65.3%/73.4%、100×768 48.0%/66.2%），重启条件①实证成立。
- **实现**：`internal/opc/saveplan.go` 的 `Write` 在 `CopyOriginal` 分支优先 raw 直通（`(*zip.File).OpenRaw()` 取原始压缩帧 + `(*zip.Writer).CreateRaw()` 原样写出），否则回退 `copyPart`；`tryRawCopyOriginal` 实现门限=仅非 XML（`*.xml`/`*.rels` 除外）、仅 `Store`/`Deflate`、无加密位(bit0)、无 data-descriptor 位(bit3)、`CompressedSize64`/`UncompressedSize64` 非 0，逐字段复制 `FileHeader`。`internal/opc/package.go` 暴露 `rawPartFile`（重启条件③所需底层 `*zip.File` 入口）。新增 `saveplan_raw_test.go`（3 媒体 raw 直通 + B1 解压内容 byte-identical）、`saveplan_recompress_probe_test.go`（基准 + 忠实性）。
- **`.git` 损坏恢复（关键纠偏）**：原诊断"stash pop 失败"有误——真因是 `.git/refs` 缺失 + `packed-refs` 缺失 + HEAD 对象丢失（与 stash 无关）。从 `logs/refs/heads/main` 末行 reflog SHA 重建 `refs/heads/main` 与 `refs/remotes/origin/main`（`f7c8dad`），再 `git fetch origin` 重建 `objects/`。`git status` 恢复后显示 Tier 2 代码本就在工作树（package.go/saveplan.go 为 modified），两测试文件 untracked——无需 stash 恢复。
- **验证全绿**：`go build ./...` RC=0；`go test ./internal/opc/ -count=1` ok；三条 Tier 2 测试 `TestSavePlanWriteTier2RawPassThrough` / `TestSavePlanWriteTier2RawPreservesSourceFrame` / `TestRawPassThroughFaithful` 全 PASS；`go vet` 干净；公开语料 B1 `TestCorpusB1UnchangedSave/(s001|s002|s003)` 全 ok（ZIP 层改动不改内容，天然兼容）。
- **提交推送**：`752fb95`（feat，SSH 签名成功，4 files/457 ins）、`27a539d`（docs: ADR-018 状态改"已实施"+ 同进程取证表 + 安全门限 + B1 兼容论证，无签名）；`git push origin main` RC=0，`f7c8dad..27a539d` 已上 origin/main；工作树干净。commit 2 无签名原因：`-S` 触发沙箱对 `C:\Users\32791\.ssh\*` 权限请求被用户拒绝；仓库 `commit.gpgsign` 未设，无签名即默认行为，无影响。
- **运维注记**：本机 `.git` 在本轮会话中又一次损坏（refs 缺失 + HEAD 对象丢失），环境稳定性需关注；后续大操作前仍应 `cp -r .git` 备份。
- **待办独立项**：`ext-0024`（B1 既有缺陷，"output has 96 entries, plan wants 75"）与 Tier 2 无关，需另立 issue/ADR 跟踪。
