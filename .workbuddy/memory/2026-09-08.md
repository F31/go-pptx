# 2026-09-08 工作日志

## go-pptx 项目实施计划制定
- 通读设计基线《go-pptx_完整设计方案_V2_6_开发实施版.md》（docs/ 下），制定并输出实施计划：`docs/go-pptx-项目实施计划.md`（V1.0）。
- 计划要点：M0–M8 阶段门禁制；约 34 个工作包映射到阶段（含 V2.6 新增 GEOM-02/STYLE-02/LAYOUT-01/ANIM-02/VIDEO-01/CHART-02/TEXT-03 七个 M6 包与 TOOL-02）；docProps(P0)/AltText(P1)/WASM CI(P1) 落位 M2/建仓；MVP= M0–M2（闭环一：模板打开→读讲稿→改文本备注→保存）；闭环二=M4（音频嵌入→计时→放映）。
- 当前状态：workspace 仅 docs/，尚无代码与 git 仓库、无记忆目录（本次已建 .workbuddy/memory）。
- 排期基线假设（待确认）：2 名 Go 开发 + 0.5–1 测试/语料，Windows 主平台，PowerPoint/WPS 首批客户端。M0–M5 中值约 30 人周；MVP 约 8–10 周（双人）。
- 开工下一步：CORE-01 建仓 + 20–30 份语料收集 + M0 垂直验证（改 1 个 Run 保同页扩展）为最高优先，失败即回方案调保存策略。

## 按实施计划开始执行（Agent 模式，M0 起步）
- git 仓库已初始化（main 分支），首个提交 f62dc03；本机 Go 1.27.0、无 gcc（race 由 CI ubuntu job 覆盖）。
- 交付：go.mod（go 1.24.0，module 占位 go-pptx）、MIT LICENSE（默认待确认）、.gitignore/.gitattributes、CI（三 OS×1.24/1.27 + CGO0 + WASM job）、README 基线记录。
- 根包 pptx：errors.go 稳定错误码全集（§20.4）、ids.go（SlideID/ShapeID）、diagnostics.go（Diagnostic/ValidationReport）。
- internal/opc（OPC-01 首批）：Scan ZIP 索引（重复/大小写重复/恶意名/预算预检）、PartName 校验、Budget、countedReadCloser 实际字节计数；覆盖率 71%。
- internal/xmlstore（XML-01 首批）：词法 Scanner（token 跨度/实体解码/注释/CDATA/PI/DOCTYPE/闭合校验/非 UTF-8 拒绝）；覆盖率 80%。
- 语料模板 testdata/corpus/README.md（24 条目标）；状态跟踪 docs/go-pptx-实施状态跟踪.md。
- 验证：go vet 干净、test 全绿、Linux CGO0 与 WASM 构建通过。
- M0 剩余：XML-01 节点索引树（NodeRecord/ns 环境/深度预算）→ XML-02 补丁 → 垂直验证（需真实语料，优先收集含动画+未知扩展样本）。

## CORE-02：远程仓库关联与 module/许可正式化
- 用户提供仓库 https://github.com/F31/go-pptx.git；远程为 GitHub 初始空仓（仅 Apache-2.0 LICENSE 的 Initial commit 95adc81）。
- **事故**：首次 merge --allow-unrelated-histories 时 git 内部 stash 失败，随后 `.git` 目录整体消失（工作区完好）。恢复：git init 重建 + 重配身份/网络/remote + 重新提交。
- 正式化：module `github.com/F31/go-pptx`；LICENSE 切换 Apache-2.0（取远程字节，sha256 一致）；example_test import 同步；README/状态跟踪更新。
- 历史：e518820（CORE-02 重建提交）← merge 863d7df 关联 95adc81；origin main = 863d7df 已推送成功。
- 网络：沙箱 https 代理 127.0.0.1:52563；git https 需 `http.sslVerify false`（吊销服务器离线）；push 认证改 SSH（git@github.com:F31/go-pptx.git，本机 ed25519 绑定 GitHub jinfeng105）。

## XML-01 节点索引树（M0 收口项之一）
- 新增 internal/xmlstore/{namespace.go,index.go,index_test.go,namespace_test.go}：
  - index.go：XMLDocument（原始字节+扁平 nodes，NodeID=下标）、NodeRecord（QName/Namespace/Source/OpenEnd/CloseStart/Attrs/Children/scope）、IndexWith(IndexOptions{MaxDepth})；ErrDepthLimit/DepthError；Elements(ns,local)/Attr(ns,local)/ContentSlice；多根报 ErrMalformed。
  - namespace.go：NamespaceScope 链（Resolve/bind/declared、默认 ns 键 ""）；isXMLNSAttr/xmlnsAttrPrefix；NS 常量（mc/DrawingML/PresentationML/relationships/content-types）。
  - 语义要点：xmlns 声明先收集再解析（元素可绑自身声明前缀）；End 后 scope 回退到父元素 scope；属性无默认 ns；等价前缀异写同 URI 语义识别（scanner 闭合仍按 rawName 词法）。
- 测试 12 组覆盖树结构/跨度/mc:AlternateContent 保留/未知子树/等价前缀/深度预算/多根/根前内容忽略/Original 共享；覆盖率 80.2%→83.8%。
- XML-01 状态：代码+单测完成（待真实语料冒烟）；README/状态跟踪已更新。下一步：XML-02 补丁引擎。

## XML-02 补丁与受控结构插入（M0 收口项之二）
- 新增 internal/xmlstore/{patch.go,insert.go,patch_test.go}：
  - patch.go：SpanPatch（Start/End/Replacement/Expect 锚定/Desc）；ApplyPatches 统一校验（范围/锚定/两两重叠，PatchError 带原始下标）+ 升序重建（与降序原地替换等价）；ErrPatchOverlap/Range/Anchor、EscapeText/EscapeAttrValue（XML 1.0 Char 校验、非法字符显式拒绝 ErrEscapeInvalidRune；空白属性逐字保留）；NewTextPatch/SetAttrValuePatch（引号从原文探测、解码原值作锚定）。
  - insert.go：InsertBefore/After/AppendChild → 零长 SpanPatch；片段必须 well-formed 单根 + 显式前缀自足或插入点作用域可解析（ErrFragmentNamespace）；自闭合 AppendChild 拒绝（ErrInsertPoint）。
- 教训：首版 ApplyPatches 用"降序回放 append"导致段序颠倒（append 先写的在前）；改为升序重建并在注释说明等价性。errors.Is 断言需 %w 包裹哨兵。
- 测试：重叠/包含/同区间冲突、相邻合法、锚定漂移、转义全矩阵、属性引号风格保持、插入三向位置、ns 依赖四态；TestPatchPreservesUntouchedRegions=垂直验证单元级雏形（改一个 Run，AC 动画分支+未知子树+CDATA 字节逐字不变）。覆盖率 86.2%。
- M0 状态：XML-01/XML-02 代码+单测完成；剩余=真实语料冒烟 + B1 垂直验证（阻塞于语料）+ QA-01。下一步（不依赖语料）：OPC-02 关系图/Content Types/主 Part 发现（M1 首项）。

## OPC-02 关系图/Content Types/主 Part 发现（M1 首项）
- 新增 internal/opc/{contenttypes.go,relationships.go,package.go,package_test.go}：
  - contenttypes.go：ParseContentTypes；Lookup 顺序=Override 精确→Override 大小写兜底→Default（扩展名小写化）；重复 Override/Default（大小写合并判定）拒绝；extensionOf 无扩展名返回空。
  - relationships.go：ParseRelationships（rId 集合内唯一、TargetMode 缺省 Internal、非法值拒绝）；resolveTarget 相对源 Part 目录逐段解析（"."消/.."上跳/越根拒绝 ErrMalformedPackage）、绝对目标 "/" 开头校验、百分号最小解码；hasURIScheme 自动 External（真实包超链接常省略 TargetMode）——由测试暴露补上。
  - package.go：Package.Load 装配（Scan+ContentTypes+全部 .rels 解析，rels 源冲突检测，缺 _rels/.rels 拒绝）；MainPart 走 officeDocument 关系（非固定名称金样：主 Part 故意叫 deck.xml）；RelatedParts/RelatedByIDs（rId→目标，供回填 XML 引用）；Walk visited 防循环（版式↔母版环安全终止）。
- 语义坑：xmlstore 属性无默认命名空间——Content Types/rels 的未加前缀属性要用 Attr("", ...) 查（初版用元素 ns 查全部落空）；"_rels/.rels" 去 ".rels" 后是 "_rels/"（源=包根）。
- 测试：Override 优先金样/大小写兜底、关系解析五态（相对/../绝对/外部/百分号）、越根与重复 rId 拒绝、Load 缺件四态、Walk 环终止（6 条关系恰访问一次）。opc 覆盖率 83.7%。
- 进度：M0 代码项全部完成（CORE-01/OPC-01/XML-01/XML-02），M1 首项 OPC-02 完成。下一步：SAVE-01 保存计划+未变 Part 复制（B1 基础），或等语料跑垂直验证。

## SAVE-01 保存计划与未变 Part 复制（M1 第二项，B1 基础）
- 新增 internal/opc/saveplan.go + contenttypes.go 写侧（clone/removeOverride/addOverride/serialize 确定性排序）：
  - ChangeSet{Patched/Added(AddedPart{Content,ContentType})/Deleted} → BuildSavePlan：唯一 PlannedEntry 清单（CopyOriginal/EmitPatched/EmitNew/Omit），按 Part 名排序确定性输出；交叉冲突（patch+delete 等）/存在性/名称校验（ErrPlanInvalid/ErrNotFound/ErrMalformedPackage 分层）。
  - CT 与变更集同源再生成（§18.2）：仅增删时 EmitPatched；Added 无 ContentType 且无 Default 覆盖 → ErrPlanInvalid；serialize 属性经 xmlstore.EscapeAttrValue(s,'"') 转义（注意签名带 quote 参数）。
  - 删除自动连带关系流（relsPartOf）；悬空关系诊断 OPC_DANGLING_REL（非阻断）。
  - SavePlan.Write：CopyOriginal 从源包读解压内容复制（B1），输出条目用 EntryName()（不能直接写 PartName——带前导 "/" 会被 PartName 校验拒绝，测试暴露）。
- B1 哈希回归测试全绿：空变更集逐 Part 哈希一致（AT-01 等价）；补丁保存（xmlstore SpanPatch 真实路径）仅目标 Part 变化；增删后其余 Part 一致 + 输出 CT 可解析；同变更集两次输出字节一致（确定性）。
- 坑：relsSource 拼接曾丢前导 "/"（诊断 Source=ppt/deck.xml）；zipindex_test.go 的 sortStrings 在测试文件中，包代码用 sort.Strings。
- 进度：M0 代码项 + M1 全部完成（OPC-01/02、SAVE-01/02、XML-01/02、MODEL-01）。MODEL-01 交付：Presentation 七方法骨架、Slide 句柄（ErrClosed/ErrStaleHandle）、revision 事务（stage/commit+ErrConcurrentModification 守卫）、库内最小合法模板（原创 theme/master/layout，待真实客户端冒烟）。
- 教训：①Go zip.Writer 全程缓冲仅 1 次底层 Write，故障注入须任意 Write 直接失败；②Windows 下覆盖已打开文件会共享冲突（Access denied），测试须先释放句柄；③本会话 Edit 工具多次静默未落盘，重要修改后必须 grep 验证，必要时改用 python 补丁。
- 下一步：M2 文本模板 MVP（TEXT-01 段落/Run 起步）；M0 垂直验证与首客户端冒烟仍阻塞于真实语料。

## SAVE-02 原子保存与失败恢复（M1 第三项）
- 新增 internal/opc/save.go：`SavePlan.SaveToFile(pk, path, opts...)`：
  - 流程：目标检查（默认禁覆盖 ErrOutputExists）→ 同目录临时文件（.go-pptx-save-*.tmp）→ plan.Write → fsync(WithDurability(DurabilityFull)) → Close → verifyOutput（重开 ZIP 校验结构+条目集与计划一致）→ os.Rename 原子替换（POSIX rename / Windows MoveFileEx REPLACE_EXISTING）→ 目录 fsync（仅非 Windows）。
  - 失败语义：提交前任何失败删除临时文件、旧目标不变（AT-11 等价）；清理失败 errors.Join 附加，不掩盖主错误；替换失败报 ErrAtomicReplaceUnavailable 且旧目标保留，绝不删旧再写（§27）。
  - WithOverwrite(v bool)/WithDurability(d) 函数式选项。
- 测试七组：基本保存可重 Load、DurabilityFull、默认禁覆盖三查（错误/目标不变/无临时残留）、显式覆盖、写入失败保留目标、目录不可写快速失败、writer 故障注入、verifyOutput 拦截缺条目输出。opc 覆盖率 84.0%。
- 坑：Go archive/zip.Writer 全程缓冲，Close 时一次性落盘（仅 1 次底层 Write）——writer 故障注入必须在任意一次 Write 直接失败，"第 N 次后失败"模式测不到。
- 进度：M1 三项 OPC-02/SAVE-01/SAVE-02 全部完成。剩余 MODEL-01（Presentation/Slide/Shape 句柄+New/Open/OpenReader/Save/Write/Close/Validate 骨架+revision 事务）。
## TEXT-01 富文本模型与备注（M2 首项，已推送 2385eeb）

- 交付：presentation.go DocumentStore 扩展（addedParts/deletedParts/partDocs 三表、docOf 泛型视图、commit 合并三类变更整体失效缓存）；font.go（Optional[T]、FontStyle、ColorSpec）；text.go（TextFrame/Paragraph/TextRun 三级句柄，nodeStep 稳定路径定位）；notes.go（SpeakerNotesText/SpeakerNotes/EnsureSpeakerNotes/SetSpeakerNotes，notesMaster 真实关系解析、rId/序号动态分配）。
- text.go 关键机制：SetPlainText 结构替换（保 bodyPr/lstStyle、未知扩展 ErrUnsupportedEdit）；SetFont 走 patch（rPr 自闭合展开 expandSelfClosingRPr、属性就地改 setRPrAttr、子元素按 rPrChildRank schema 序插删换）；ResetFontProperty → removeAttrPatch/removeElementPatch + removeEmptyRPr（空 rPr 整删）；句柄每次操作按 [ns,local,nth] 路径重定位（祖先被删 → ErrStaleHandle）。
- 本次修的两个真 bug：① xmlstore AttributeRecord 原来只有 ValueStart/End 无属性名锚点 → removeAttrPatch 用文本反向搜索会误匹配值内同名文本，且删除区间不含结束引号（ValueEnd 指向引号前）产生 `<a:rPr"` 残引号 → index.go 增补 NameStart/NameEnd 字段（scanner Attr 早已有，仅 index 拷贝时丢失），删除区间 [NameStart-空白, ValueEnd+1]。② pathToRun 只拼到 p 没拼 r 步骤 → removeEmptyRPr 在 p 下找不到 rPr。
- 另：Save/Write/Validate 对 nil ctx 兜底 context.Background()（text_test 传 nil 触发 panic 发现）。
- 验证：go vet/test ./... 全绿；根包覆盖率 49.1%；GOOS=js/wasip1 + Linux CGO_ENABLED=0 交叉构建通过。
- 下一步：M2 第二项按实施计划 §12 顺序（状态表 TEXT-01 后一项）。M0 垂直验证仍阻塞于真实 PPTX 语料。
## TEXT-02 跨 Run 替换与整批变更（M2 第二项，将推送）

- 交付 replace.go：Paragraph.ReplaceText + TextFrame.ReplaceText。核心机制：
  - 逻辑文本视图与 Paragraph.Text() 一致（普通 run a:t 解码拼接）；段落按 segBlock 划分（a:br/a:fld 硬边界、run 的 hlinkClick/hlinkMouseOver key 不同即分块）→ 匹配只在块内、从左到右非重叠、基于初始快照、不递归。
  - 三种 ReplaceMode：FirstCharacter（默认，rep 继承命中首字符 run 的 rPr；单 run 命中且替换后文本非空 = 原地 t 替换不拆 run；跨 run/空文本 = 前缀 run+rep run+后缀 run 重建区间）、EqualLengthPerRune（rune 数一致否则报错；逐 run 等长改 t 不拆 run 保留链接）、ExplicitStyle（WithReplacementStyle 必填）。
  - 占用 Run 区间贪心调度（先到先得）：rebuild 型占 [ri..rj]、inplace 型占单/多 run 但同 run 多命中合并为一个 t 补丁 → 所有补丁区间互不重叠 → 一次 ApplyPatches + 单次 stagePatch/commit（整批变更）。
  - 字素簇保护：graphemeSafe 拒绝匹配起点为 Mn/Mc/Me、终点后紧跟组合标记、切 ZWJ。
  - 安全约束：重建/删除 run 前检查 run.link 或直接子元素 ∉ {rPr,t} → 跳过（Reason=unsafe-link-or-extension-run / adjacent-match-run-conflict）。
- 修的坑：① locateBlockSpan 的 rj 判定最初用 "gsi<hi"（命中起点在某 run 内）→ 跨 run 命中只定位到首 run；应 "gei<=hi"（命中终点所在 run）。② OperationError.Error() 只输出 Err 忘了 Message 字段 → 补 Message 前缀（errors.go，fmt import 移除）。③ 同 run 多命中（"x1x2x" 3 个 x）每个 t 整段替换 patch 区间相同 → 合并 runChanges 降序应用生成单 patch。④ 测试源码里 \u0301 经 Write/heredoc 转义歧义变字面 → 用 string(rune(0x0301)) 构造组合字符。
- 验证：vet/全仓测试全绿；根包覆盖率 49.1%→57.3%；WASM(js+wasip1)/Linux 交叉构建通过。
- 下一步：M2 第三项（状态表 TEXT-02 后 = STYLE-01 基础占位符与样式解析）。
## STYLE-01 基础占位符与样式解析（M2 第三项，将推送）

- 交付 style.go + style_test.go：TextRun.EffectiveFont(ctx ResolveContext) (ResolvedFont, []Diagnostic, error)。
- 公共结果类型：ResolvedValue[T]{Value/Resolved/Fallback/Trace}、ResolvedColor{Spec(原始)/RGB/Resolved/Fallback/Trace}、StyleStep{Source/Part/Detail}、StyleSource 枚举（run/paragraph-default/list-style/theme/fallback）。ResolveContext{Fallback FontStyle（Optional 逐字段，仅 Set 采用并标 Fallback=true）; Strict（任一属性未决 → ErrUnresolvedStyle）}。
- 解析链（每属性族独立取首个显式值）：L1 run rPr → L2 pPr defRPr → L3 列表级别（段落 pPr@lvl 缺省 0；占位符形状：layout 匹配 ph lstStyle → master 匹配 ph lstStyle → master txStyles[class]（title/ctrTitle→titleStyle、其余 ph→bodyStyle、非 ph→otherStyle、notes→notesStyle）；非占位符直接从 otherStyle）→ L4 主题缺省字体（class title → majorFont，否则 minorFont；主题 ea/cs 为空 → 如实 unresolved）。layout 无 txStyles（只有 master/notesMaster 有）——链里 layout 只提供占位符形状 lstStyle。
- 占位符匹配：phKey 规范化 (type 缺省 "obj", idx 缺省 0) 全等（ECMA CT_Placeholder 默认），findPlaceholderShape 遍历 spTree，不按坐标/名称。
- 导航：styleEnv 沿真实 rels（slide→RelSlideLayout→layout→RelSlideMaster→master→RelTheme→theme；notesSlide→relNotesMaster→notesMaster→theme）。新增 p.relsOf(part) 读视图（overrides 中的 rels 补丁优先，其次包内流；用 relsPart/ParseRelationships），修复 notesPartOf 旧实现只看 pk.Relationships 导致 createNotes 后同会话 SpeakerNotes 找不到 notes（测试 TestEffectiveFontNotesChain 回归验证）。
- 颜色：srgbClr 直出；sysClr lastClr 优先、否则 windowText/window 表；schemeClr bg1/bg2/tx1/tx2 经 master p:clrMap 映射（如 tx1→dk1）；clrScheme 条目子元素取 srgbClr/sysClr + 按文档序应用 lumMod/lumOff/shade/tint（val 千分比 0..100000，公式：lumMod R*pct；lumOff R+255pct；tint 向白 R+(255-R)pct；shade 向黑 R*(1-pct)）；phClr/未知变换/未知 scheme/未知 sysClr → STYLE_PARTIAL（Resolved=false，可能带基础色）。字体 "+mj-lt"/"+mn-lt" 等 → theme fontScheme major/minor 的 latin/ea/cs typeface。
- 诊断 code：STYLE_UNRESOLVED（链上无值且无回退）、STYLE_PARTIAL（有引用但无法完全解析）、STYLE_STRICT（strict 汇总）。ExplicitFont 保持只读本地 rPr（Set=false=无本地覆盖），继承解析一律走 EffectiveFont。
- 测试链 fixture（style_test.go）：theme accent3 加 lumMod 60000（A5A5A5→636363）、dk1 sysClr windowText lastClr 000000；master 放 body idx1/idx2 占位符形状（lstStyle 各带 defRPr）+ txStyles（titleStyle/bodyStyle lvl1+lvl3/otherStyle）；layout 放 body idx1（defRPr b=1 sz=2400）。覆盖：title 全链（44pt/#000000/Calibri Light/trace Part=master titleStyle）、body idx1 layout+master 分层、run rPr 覆盖（b=0 sz=2000）、lvl=2 走 bodyStyle lvl3（12pt、tx1→dk1 色经 clrMap、trace 含两 Theme 步）、body idx2 仅 master 命中（30pt/ED7D31）、普通文本框 otherStyle（18pt，color 未决→fallback FF0000 标 Fallback；Strict→ErrUnresolvedStyle）、phClr（STYLE_PARTIAL）、notes 链（latin=minorFont Calibri、color/size 未决）+ SpeakerNotesText 回归、closed doc→ErrClosed。
- 验证：vet/全仓测试全绿；根包覆盖率 57.3%→73.1%；WASM(js+wasip1)/Linux CGO_ENABLED=0 交叉构建通过。
- 下一步：M2 第四项 = IMAGE-01（状态表 STYLE-01 后一项）。
## IMAGE-01 PNG/JPEG 图片与媒体暂存（M2 第四项，将推送）

- 交付 media.go + picture.go + image_test.go：
  - MediaSource 契约（Open 每次新流/DeclaredType）+ FileMedia(path)（与方案 §28 单参签名一致）/BytesMedia/FuncMedia/ReaderMedia（有界复制 512MiB）；probeImage 魔数+DecodeConfig，错误分类：非 PNG/JPEG→ErrUnsupportedFormat、损坏→ErrInvalidArgument、声明 MIME 不一致→ErrInvalidArgument（image/jpg↔image/jpeg 等价）。
  - Slide.AddPicture(ctx, src, spec)：四 Fit（OriginalSize=px×9525@96dpi / Stretch / Contain 等比缩小居中 / Cover 用 a:srcRect 裁剪不重采样，srcRect 千分比 l=r=(1-W/(s·wE))/2·100000）；cNvPr id 递归 max+1、name "Picture N"；AltText→@descr、IsDecorative→@decorative="1"（互斥，§8.1）；spTree AppendChild 置顶；返回 PictureShape 句柄（nodeStep 定位同 text 模型）。
  - ReplaceImage：保留几何清除旧 srcRect；去重（SHA-256+CT 限定 /ppt/media）→ 复用或 stageAdd imageN.ext（newMediaName 跨扩展全局递增，兼顾 pk+addedParts 防撞名）；**关系与 XML 变更合并为同一基线一次补丁**（避免两次 stagePatch 互相覆盖）；本页 blip 计数（skipID=blip 节点自身 ID，非 pic ID！）无其它使用者才删旧 rel 条目；mediaTargetRefs（pk+addedParts 全源保守计数）归零才 stageDelete 旧媒体。
  - stageRelsBytes：rels 流 Part 不存在（pk 与 added 均无）时用 stageAdd 而非 stagePatch。
- 本次修的真 bug：
  ① **relsXML 只读 pk 忽略 overrides/addedParts** → 会话内已新增的 slide rels 被当"空流"，nextRID 重算回 rId1 → insertRel 用 rId1 覆盖原条目（旧媒体关系被静默改写）；修复为与 relsOf 同源的读视图。② commit() 对会话新增（addedParts）Part 的 stageDelete 会写 deletedParts，保存时 crossCheck 报"deleted part 不存在"；修复：pk.HasPart 才写 deletedParts，否则直接丢弃（输出从未含它）。③ countBlipsEmbed 的 skipID 误传 pic.ID（blip 恒被计入→旧关系永不删除）；改传 blip.ID。④ FileMedia 初版两参偏离方案 §28 单参契约，收敛。
- 验证：vet/全仓测试全绿；根包覆盖率 73.1%→75.2%；WASM(js+wasip1)/Linux CGO_ENABLED=0 交叉构建通过。测试含：四 Fit 几何断言、alt/decor、同页+跨页去重、共享引用保护+孤儿清理、同内容 no-op、Save 往返重开、FileMedia 调用时固化源字节。
- 下一步：M2 第五项 = 页面 API（Slide.Shapes/AddShape 等，状态表 IMAGE-01 后一项）。M0 垂直验证仍阻塞于真实 PPTX 语料。
## docProps 文档级元数据（5.1，M2 附加项，将推送）

- 交付 docProps.go + docProps_test.go：CoreProperties（Title/Subject/Author/Company/Keywords/Category/Comments Optional[string] + Created/Modified Optional[time.Time]）、CorePropertiesPatch、CoreProperties()/SetCoreProperties、CustomProperties()/SetCustomProperty。
- 字段映射：Title→dc:title、Subject→dc:subject、Author→dc:creator、Keywords→cp:keywords、Category→cp:category、Comments→dc:description、Created/Modified→dcterms:created/modified（W3CDTF）；Company 非 core 标准字段，落 app.xml Properties/Company（扩展属性），core.xml 缺失时不读 Company。
- 语义关键点：① Optional patch 不清空未提及字段；② Modified 默认"库代管"：Presentation 新增 coreAutoModified 字段，Save/Write 前 flushAutoModified() 把 core.xml dcterms:modified 刷为当前 UTC（同秒 no-op），每次 SetCoreProperties 独立判断（显式带 Modified → 固定值并清代管；未带 → 恢复代管）；③ core.xml 缺失：读零值 nil、写按需创建 Part + 包根 .rels 关系（ensureRootRel，rId 动态）+ CT Override（stageAdd with ct → 保存计划自动生成，勿手工改 [Content_Types].xml）；④ custom.xml 同按需创建；⑤ SetCustomProperty 同名覆盖保留 pid、新 pid=max+1 起始 2、fmtid 固定 GUID {D5CDD505-2E9C-101B-9397-08002B2CF9AE}；⑥ i4 超 int32 拒绝（ErrInvalidArgument）、未知变体读取 ErrUnsupportedFormat。
- 插入机制：缺失字段按 ECMA CT_CoreProperties 字母序位次（category0..version14）排序，逐字段找锚（首个位次更高的既存已知元素）；**多根 fragment 不被 xmlstore 受控插入接受** → 以手工 SpanPatch 分组插入（叶已 EscapeText、前缀自足），锚分组+root 末尾组。
- 本次修的真 bug：**commit() 对会话新增（addedParts）Part 再次 stagePatch 后，addedParts.Content 未同步** → buildPlan Added 优先于 Patched，输出仍是旧 Added 字节（docProps 刷新 modified 静默丢失）；修复为 Patched 循环内同步 a.Content=b。此 bug 在 IMAGE-01 阶段就潜伏（ReplaceImage 对刚 stageAdd 的媒体理论上可复现），docProps 保存往返测试暴露。
- 测试注意：构造"缺失 core.xml/custom.xml"fixture 时除删 Part+根 rels，还须删 [Content_Types].xml 对应 Override（否则保存计划再生成 CT 时报 override already exists）。
- 验证：vet/全仓测试全绿；根包覆盖率 75.2%→76.0%（docProps.go 主干 83-100%）；WASM(js+wasip1)/Linux 交叉构建通过。
- 下一步：M2 收口 = AltText(8.1，Picture 部分已随 IMAGE-01 完成，余 AutoShape 待形状 API) 与页面 API（Slides/Layouts/AddSlide/MoveSlide/RemoveSlide/Shapes/Placeholders、ErrForeignReference）。按实施计划 M2 剩余顺序 docProps 后是页面 API 收口。

## 页面 API 收口 + 形状枚举/AltText(8.1)（M2 代码收口，推送 6d5404e 后）

- pages.go：Slide(index)/Layouts(LayoutRef)/AddSlide/MoveSlide/RemoveSlide。
  - AddSlide id 在 [256,2^31-1] 最小空闲（sort 后补隙扫描，勿线性扫 2^31）；自闭合/缺失 sldIdLst 展开技巧：自闭合替换补丁 Start=OpenEnd-2,End=OpenEnd,Replacement=">"+frag+"</p:sldIdLst>"。
  - MoveSlide 最终位置语义：j<i 插到原 L[j] 前；j>i 插到原 L[j+1] 前或列表尾；删除区间与插入点不重叠→两组 SpanPatch。
  - RemoveSlide 未知依赖阻止扫描遍历 pk+addedParts 全部非 .rels 源，仅放行主 Part 与待删 notesSlide；notesMaster 保留。
  - 修复 nextPartSeq 未扫 addedParts（同会话重复建 notes/slide 撞名 latent bug）。
- shapes.go：shapeNode 通用形状句柄基元（locate + ID/Name/AltText/IsDecorative + setAltText/setDecorative(op,..)）；elementCNvPr 按元素名推导 nv*Pr 容器（nvPicPr/nvSpPr/nvGrpSpPr/nvCxnSpPr/nvGraphicFramePr）；shapePhKey 泛化占位符键；Shapes/Placeholders 枚举跳过 nvGrpSpPr/grpSpPr；AutoShape(txBox 判别)/OpaqueShape(只读)；PictureShape 重构嵌入 shapeNode（image 测试全绿，cNvPrOf→elementCNvPr，错误 "errors" import 移除）。
- Slides() 改 relsOf 读视图（原 pk.Relationships 只见原始包，AddSlide 后不可见新增页）。
- 测试：pages_test 9 + shapes_test 6；withSlides 简化夹具无 slide .rels，备注/删除场景需 deckSlidesWithRels（slide .rels 必须存在否则 createNotes 的 stagePatch 落空→save 报 patched part not exist）。
- 覆盖率 76.0%→76.3%；vet/全仓测试/Linux CGO=0/GOOS=js+wasip1 全过。
- 注意：TextFrame 无 Text()，读文本要走 Paragraphs→p.Text()。
