# 2026-09-16 工作日志

## ext-0024 缺陷立项 → 挖出并修复 Tier 2 高危回归（ADR-024）
- 用户指令"先把 ext-0024 B1 缺陷立项"；实际立项过程推翻了两轮早期结论，最终定位为 **ADR-018 Tier 2 引入的真实缺陷**（非"口径问题"、非"既有缺陷"）。
- **三轮取证（每轮推翻前轮，结论留痕）**：
  1. 第 1 轮误判：凭"源 91 目录条目 = 75 文件 + 16 目录"、输出 96 → 推断 `96 = 91 + 15`（SPD 溢出），据此刻了 ADR-024 初版称"非缺陷/口径问题"（文件名 `ADR-024-ext0024-entry-count-baseline.md`）。**该版全文作废并重写。**
  2. 第 2 轮证伪：`git show b00df5d:internal/opc/zipindex.go` 显示 SAVE-01 初版起 `Index.Scan` 就已 `HasSuffix(f.Name,"/") → continue`，目录条目**从不**进 `plan.Entries`；数值巧合属误导。重开输出 ZIP 逐条计数，发现**真实重复条目**（21 个 media 各出现 2 次）。
  3. 第 3 轮隔离：注入实验定位到 `SavePlan.Write` 的 `CopyOriginal` 分支。
- **根因**：`SavePlan.Write`（`internal/opc/saveplan.go:254`）原在循环开头**无条件** `zw.Create(entry)`；Tier 2 raw 直通成功后 `tryRawCopyOriginal` 又调 `zw.CreateRaw` 注册**同名第二个条目**（`archive/zip` 允许同名重复）。实测**连 XML Part 也重复**（`zw.Create` 无条件执行），即**每条 `CopyOriginal` 都产出两条**——任何含媒体的文档 `Save`/`SaveToFile` 均失败（`verifyOutput` 报 `output has 96 entries, plan wants 75`；`Load` 报 `duplicate entry`）。Tier 2 自 `752fb95` 落地即损坏。
- **修复**：`zw.Create` **下沉**到 `EmitPatched/EmitNew` 与「raw 回退」两个真正需要的分支；raw 成功路径只由 `tryRawCopyOriginal` 的 `CreateRaw` 注册。未放宽 `verifyOutput`、未放宽 raw 门限（拒绝把门禁调松来"修"缺陷）。
- **守门（防回归）**：
  - 新增 `TestSavePlanWriteNoDuplicateEntries`（`saveplan_raw_test.go`）：按 `zr.File` **逐条计数不经 map**、断言条目数 = 计划非 Omit 数、断言 `opc.Load` 接受输出。
  - `real_corpus_test.go` 追加 `assertEntryCountInvariant` helper + 在 `TestRealExt0024_SaveUnchangedB1` 末尾调用：逐条计数（跳目录条目）、Part 名集合与 plan 逐项相等、断言 `physical >= len(wantParts)`（允许 SPD 溢出，不允许丢条目）。
  - **注入验证**：把 bug 加回代码确认守门必红（`duplicate entry "ppt/media/bench2.bin" appears 2 times` / `output entries = 12, want 6` / `Load rejected`）。
- **验证**：修复后 `75 plan parts, 75 physical entries`、75 Part 字节恒等、`SaveToFile round-trip OK on real sample: 5964704 bytes`（修复前为 `output has 96 entries, plan wants 75`）；`go test ./...` + `-tags=corpus ./...` 各 14/14、`go vet` 两 tag 全绿。
- **文档同步（5 份）**：新建 `docs/adr/ADR-024-saveplan-duplicate-entry-fix.md`（原 `...-entry-count-baseline.md` 经 `Move-Item` 更名，内容全重写）；`ADR-018` 状态行 + 验收清单第 4 项（"与 Tier 2 无关"改为"Tier 2 引入的真实缺陷，已修复"）；`v1.0-bug-registry.md` 新增 §1.5 + §4 backlog 划掉闭合；`go-pptx-实施状态跟踪.md` 首行更新 + 关联文档追加；`CHANGELOG.md` `[Unreleased] ### Fixed`。

## 测试盲区类型学（本次最大教训，4 层全漏）
1. **`map[name][]byte` 收集条目** → 同名重复互相覆盖（`rawParts` helper，`saveplan_raw_test.go:76`），**盲区根源**（未改，新测试绕开）。
2. **CI 上私有语料恒 Skip** → 真实样本守门在 CI 永不执行。
3. **末段仅 `t.Logf` 无断言** → 观察到了也没红。
4. **`Write` 单测自建 `zip.NewReader` 不校验重复** → 只验自己能读，不验条目集契约。
- **对策**：性能优化改动（尤其 ZIP/IO 层）必须跑 `SaveToFile` **端到端**校验（`Load` 接受 + 条目集比对），不能只跑 `Write` + 自建 `zip.NewReader`。计数断言须**逐条计不经 map**。

## 环境/工具经验
- `go fmt ./...` 又把 3 个 CRLF 文件（`errors.go`、`scripts/perf/summarize/main.go`、`main_test.go`）整体重写为 LF；`git diff --numstat` 确认零内容 hunk 后 `git checkout --` 还原。
- 探针文件（`zz_repro_test.go` / `zz_diag_test.go` / `zz_isolate_test.go`）经 `git clean -f -n` 干跑确认后 `git clean -f` 清除。
- PowerShell 外部命令 stdout 不回显 → `| Out-File -Encoding ascii/utf8 $env:TEMP\x` 再 `Read`；`Read` 对未指定编码的重定向文件会当二进制，须显式 ascii/utf8。

## COV-04 覆盖率门槛回归（ADR-018 Tier 2 第二处同源缺陷）
- **发现**：例行全仓覆盖率复核发现 `internal/opc` **89.5%**（登记值 90.0%），跌破 COV-04 的 90% 门槛。
- **归因（worktree 隔离实测）**：`f7c8dad`（Tier 2 之前）90.4%/90.5%；`27a539d`（Tier 2 已落地、ADR-024 未修复）89.6%；HEAD 89.5%/89.6%。→ **降幅由 Tier 2 引入（−0.8pp）**，ADR-024 的结构调整无额外影响。**Tier 2 自 `752fb95` 落地即跌破门槛，三天无人发现**。
- **根因**：`tryRawCopyOriginal` 的 4 条安全门限拒绝分支零测试覆盖（该函数覆盖率仅 73.1%）。
- **修复（零生产代码改动）**：新增 `internal/opc/saveplan_raw_guard_test.go`
  - `TestTryRawCopyOriginalSafetyGate`：8 case 表驱动（合格 Store / `.xml` / `.rels` / 缺失 Part / method 99 / 加密位 / data-descriptor 位 / 声明尺寸 0）+ 断言"被拒帧不得在输出注册任何条目"
  - `TestSavePlanWriteFallsBackOnUnsafeFrame`：data-descriptor 帧端到端验证回退路径
  - 3 项写入错误注入（raw 直通 / 流式回退 / `zip.Close` flush）
- **结果**：`internal/opc` 89.5% → **90.3%**（corpus 89.6% → **90.5%**）；全仓 total 84.4% 不变。
- **守门有效性**：注入验证（临时删加密位门限）→ `reject-encrypted-frame` 必红（`raw = true, want false`），已还原（`saveplan.go` 零 diff）。

### 关键构造技巧（可复用）
- `zip.Writer.Create` 产出的条目**天然带 flags bit3**（Go 用 data descriptor 回填尺寸）→ 合成包条目全部被 Tier 2 门限拒绝 → 这**正是"不安全帧"的真实形态**，可直接用于回退路径测试。
- 反之，要产出**可 raw 直通**的帧必须用 `CreateRaw` + 显式 `CompressedSize64`/`UncompressedSize64`（Tier 2 现有测试即如此构造）。
- `zip.Writer` **无法**产出"未知 method / 置位加密位 / 声明尺寸 0"的帧 → 必须手工拼 ZIP（新增 helper `rawHdrZip`：local header + central directory + EOCD，可精确控制 method/flags/size）。
- 空 ZIP 的 EOCD 固定 22 字节 → 断言"未注册条目"必须解析 `zr.File` 计条目数，**不能**断言字节长度。
- 手工构造 `.rels` 条目时内容必须是合法关系流（用 `miniRels("")`）；乱写会被 `Load` 判 malformed。

### 教训
- **Tier 2 的两处缺陷同源**：落地时只跑 build/test/vet，**既没跑 B1 端到端保存校验（→ 漏掉重复条目），也没跑覆盖率门槛（→ 漏掉门限分支零覆盖）**。性能/IO 改动的完成定义必须含这两项。
- 覆盖率复核要**定期实测**，不能只看"上次登记值"——`internal/opc` 的 90.0% 是 09-12 的数字，09-14 的 Tier 2 已把它拉下去。**"已闭合的里程碑"仍可能被新代码悄悄突破**。

## v1.0.5 发布（执行）
- **范围**：v1.0.4（`edc08af`）→ HEAD 共 **12 commit / 39 文件（+2291 / −109）**。三条工作线：① ADR-018 Tier 2 原始帧直通；② A-2 窄接口（ADR-021）+ WASM GA（ADR-023）；③ Tier 2 两处缺陷修复（ADR-024 + COV-04）。
- **公共 API**：Stable 段 34→40、符号 50→60、方法 129→131、Experimental 5→0、总 type 158→163；binary-compat with v1.0.0–v1.0.4。
- **发布前盘点的关键发现**：A-2（`15137ca`）与 ADR-020 的 commit 落在 **v1.0.4 tag 之后** —— 即它们**不在 v1.0.4 里**（"v1.0.4 = 零生产代码改动"仍成立），且此前**未进入任何 CHANGELOG 段**。本次在 [1.0.5] 段补登记。`v1.0-bug-registry.md` §1.4 的"v1.0.3 / v1.0.4 期间"是**时间口径**（09-12/13 两日）而非版本口径——已在 release notes 与状态跟踪里澄清。
- **L3 真机矩阵（第二轮，Tier 2 产物）8/8 通过**：因 Tier 2 改变输出字节，按 ADR-018 验收清单第 5 项重跑。用 `scripts/gen_corpus/gold_replace.go`（`//go:build ignore`）经 SDK 重新生成 4 个编辑后产物到 `.l3-output/t2/*.t2.pptx`，再以等价的一次性 PowerShell runner（`Start-Job` + 55s 超时；`run_client.sh` 是 WSL 版，本机不适用）驱动 `scripts/l3/ppt_open_resave.ps1`。PowerPoint 16.0.20326 + WPS 12.1.0.28599 × 4 样本：全部 `OPEN=ok`（无修复提示）+ `SAVE=ok`，重存文件 `Validate` errorCount=0，总耗时 13.3s。**这是 Tier 2 安全门限的真机端到端背书**。
- **门禁全绿**：测试 14/14（默认 + corpus）、`go vet` 双口径、`gofmt -l` 干净、四目标交叉构建（js/wasm + wasip1/wasm + darwin/arm64 + linux/arm64）、覆盖率 root 合并口径 84.4% / opc 90.3%。
- **产物**：commit `7d14f5b` + tag `v1.0.5`（SSH 签名 annotated；`v1.0.5^{commit} = 7d14f5b`）已推 origin。新建 `docs/RELEASE-NOTES-v1.0.5.md`；CHANGELOG / 状态跟踪 / freeze-list / client-compat-matrix 同步。
- **新环境经验**：① `cmd/pptx replace` 是**占位实现**（不真正写文件，只出 validate 报告）——生成编辑后产物必须用 `scripts/gen_corpus/gold_replace.go`；② Go 程序不认 MSYS 路径 `/e/...`，私有样本要传 Windows 原生路径 `E:\...`（bash 里用单引号包裹）；③ 本机 `mkdir` 同样不可用（coreutils 损坏），建目录走 PowerShell `New-Item`；④ `git tag -s` 需要读 `~/.ssh` 私钥，会触发沙箱升级（本次已批准）。

## 配音样本制作（V2.6 §15.3 第 3 条语料缺口）——并挖出 PowerPoint 拒收缺陷
- **目标**：§15.3 第 3 条要求"配音功能必须有播放记录"，而语料 36 个 manifest 无任何 audio 标签样本 → 无从验证。
- **制作方法（已实测可用）**：用 SDK 生成（与 `scripts/gen_corpus/gold_replace.go` 同款 `//go:build ignore` 模式）——
  1. 代码**合成 WAV**（16-bit 单声道 PCM，440 Hz 正弦 + 50 ms 淡入淡出）→ `pptx.BytesMedia(wav, "audio/wav")`：完全自包含、可再分发、无外部依赖；
  2. `slide.AddAudio(ctx, src, AudioSpec{TrackKey, Role: AudioRoleNarration})`——时长由 probe 推导（WAV 可推导，MP3 亦可；推导不出且未显式给 `Duration` 会返回 `ErrDurationUnknown`）；
  3. `as.SetPlayback(PlaybackSpec{Trigger: PlaybackOnSlideEnter})`；
  4. `slide.SetAdvanceAfter(音频时长 + padding)`；
  5. `p.Save(ctx, out)`。
  - **前提**：模板必须**无动画**——`SetPlayback` 对含非纯音频 timing 的页面返回 `ErrTimingConflict`（AT-05）。`s001-text` 干净可用（ext-0024 有 16 个 timing 节点，不可用）。
  - 工具与产物都在 `.l3-output/`（gitignore，不提交）：`zz_genaudio.go`（生成器）、`zz_dumpzip.go`（ZIP/XML 取证工具，`-filter` + `-find` 窗口）。
- **产出**：`.l3-output/s001-text.narrated.pptx`——WAV 176444 B / 2.0 s；`docProps/audio.xml` 记 `durMs="2000"`；slide1.xml 含 audio `p:pic` + `p:timing` 音频节点；`p:transition advTm="2500"`。
- **关键发现（高危）**：**WPS 打开正常**（识别到 `Audio 11` 形状、`advanceTime=2.5` 生效、重存 OK），但 **PowerPoint 报 `0x80070570 文件或目录损坏`**。对照实验排除环境因素：同一台机、同一 COM 会话下，原样本 `s001-text.pptx` 与 Tier 2 产物 `s001-text.t2.pptx` 均 `OPEN=ok`，**只有配音产物失败**。
- **根因（源码已定位）**：`buildAudioPicFragment`（`audio.go:466`）产出的 `p:pic` 有**两处 OOXML schema 违反**：
  1. `<p:nvPicPr>` 缺必需的 `<p:nvPr/>`——同文件里 `p:nvSpPr` 都带 `<p:nvPr/>`，唯独音频 pic 没有；
  2. `<a:audioFile contentType="audio/wav"/>` 缺必需的 **`r:link`**，且被放在 `p:blipFill` 内——PowerPoint 的原生形态是把 `<a:audioFile r:link="rIdX"/>` 放在 **`p:nvPr`** 内，而 `a:blip` 指向封面图。
  代码注释自称"powerpoint 接受的 audio 形状表达之一"——该假设**未经真机验证**，与 ADR-024 / Tier 2 同源的教训（推理代替实测）。
- **影响**：**AUDIO-01/02 的产物在 PowerPoint 下不可用**；§15.3 第 3 条的缺口性质由"环境型（缺语料）"**修正为"真实代码缺陷"**（`docs/release-readiness-2026-09-12.md` 原判断"不是代码缺陷"被实测推翻）。WPS 宽容，故代码级测试与 WPS 验证都未暴露它。
- **顺带发现的次要缺陷**：`AudioShape.Profile()` 走 `lastProfileByMedia`（`audio.go:334` 手写内联解析）**漏读 `durMs`/`stMs`/`trigger`/`slide`**，而 `PlanTimingSync` 走 `parseAudioProfile`（完整解析）——两条路径不同步。后果：`Profile().Duration` 恒为 0（用户读不到时长），但主链路 `PlanTimingSync`/`ApplyTimingPlan` 正常（实测 `PLAN err=<nil> jumps=1`）。修法：`lastProfileByMedia` 复用 `parseAudioProfile`。
- **待办**：① 立项修 `buildAudioPicFragment`（须真机复验 + 回归守门）；② 修 `lastProfileByMedia` 重复解析；③ 样本正式入 corpus（`audio` 标签 + 可再分发合成音）需 opencode 配合；④ §15.3 第 3 条"播放记录"的最终证据仍需人工录屏或 Windows 音频会话枚举。

## ADR-025 实施：修复 PowerPoint 拒收配音产物（commit `9cb0c77`）
- **修复两处**：① `buildAudioPicFragment`（`audio.go`）补 `p:nvPr`，把 `<a:audioFile r:link="rIdX"/>` 移入其中；`p:blipFill` 保留 `a:blip` 引用并补 `<a:stretch><a:fillRect/></a:stretch>`；不再写自造 `contentType`。② `audiotiming.go` 新增 `alternateContentTransitions` helper 展开 `mc:AlternateContent` 的 Choice/Fallback，把 `advTm` 写给**全部**既有 transition；仅当确实无 transition 时才新增节点。选**保真方案 v6**（保留源模板 `spd`/`p14:dur` 与 mc 结构），符合"未触碰区域不动"；mc 两分支语义等价故都写同一份 `advTm`（否则降级分支会丢自动翻页）。
- **最小实验矩阵（本轮最有价值的方法）**：写补丁工具 `zz_patchslide.go` 对**生成物**做定点增删（完全不碰生产代码），快速证伪假设——
  - 首轮假设（缺 `p:nvPr` / 缺 `r:link`）**被实测证伪**：v1/v2/v3 三种写法仍失败；
  - e3（只删 AlternateContent）✗ / e4（只删 advTm）✗ / e5（修 pic + 删 timing）✗；
  - **e2（修 pic + 删 AlternateContent）✅、v6 ✅、v7 ✅** → 确认两处缺陷是**"与"关系**（缺一即失败），其余变体恰好逐一排除其它假设。
  - 比"改生产代码 → 重编译 → 重生成 → 重测"快得多，是**可复用的取证手段**。
- **真机复验**：PowerPoint 16.0.20326 与 WPS 12.1.0.28599 均 `OPEN=ok` + `SAVE=ok`；音频形状**均识别为 type=16 (msoMedia)**、`advanceTime=2.5` 正确读出（修复前 WPS 只认作 type=13）→ **两个客户端语义达成一致**，是修复有效的强证据。
- **守门**：`audio_ooxml_compliance_test.go` 两条测试（pic 结构合规；`p:transition` 恰 2 个且 `advTm` 写全 2 处）；**注入退化验证必红**（报 `p:transition count = 3, want 2` + "a direct-child transition was appended"）。
- **文档**：ADR-025 新建；CHANGELOG `[Unreleased]`；实施状态跟踪 + `v1.0-bug-registry` 加条目；`release-readiness-2026-09-12.md` 加勘误（**推翻其"不是代码缺陷"的判断**）。
- **全绿**：`go test ./...` + `-tags=corpus ./...`、`go vet` 双口径、`gofmt -l`。
- **可复用工具**（`.l3-output/`，gitignore 不提交）：`zz_genaudio.go`（造含配音样本）、`zz_patchslide.go`（对产物做定点增删以二分定位）、`zz_dumpzip.go`（列 ZIP 条目 + `-find` 打印子串窗口，XML 单行很长时必需）。

## ADR-025 续：读取侧同源修复 + Profile 字段 + 端到端往返守门（commit `be516df`）
- **第 2 项（Profile 丢字段）**：`lastProfileByMedia` 改为复用 `parseAudioProfile`（消除手写精简副本），`Profile().Duration` 不再是 0。守门 `TestAudioShapeProfileExposesDuration`；**注入退化验证必红**（`Duration = 0s, want 2s`）。
- **第 3 项（原计划"样本入 corpus"）→ 改为更优方案**：**不放二进制样本**，改加端到端往返测试 `TestNarratedDeckRoundTrip`（合成音现场生成 → 保存 → 重开 → 断言形状 / `Profile` / `AdvanceAfter` 完整往返）。理由：自包含、CI 恒可执行、**且天然避开 `testdata/corpus/` 的 opencode 归属问题**。
- **往返测试立刻抓出 3 处读取侧同源缺陷**（"写入修好了、自己却读不回"）：
  1. `picMediaKind`（`shape.go`）只在 `p:blipFill` 里找 `a:audioFile` → 写入侧改到正确位置后**读不回自己写的 `AudioShape`**（我引入的回归）→ 改为先探 `p:nvPicPr/p:nvPr`，再回退 `p:blipFill` 以兼容旧产物。
  2. `classifyShape` 构造 `AudioShape` 时**不填 `profile`** → 读回后 `Profile()` 全零值、`AudioSource()` 报 `ErrNotFound` → 改为按 `cNvPr@id` 从 `/docProps/audio.xml` 取回。
  3. `Slide.AdvanceAfter()` 与写入侧犯同样的错（只扫直接子元素）→ 读不到 mc 包裹的 `advTm` → 复用 `alternateContentTransitions`。
- **一次"顺手改动"被测试拦下**：我曾把 `p:videoFile` 的命名空间从 `nsPresentationML` 改为 `nsDrawingML`（按 schema 它应是 DrawingML），结果 `TestSlideAddVideo_PicClassifiedAsVideo` / `TestSlideClone_PreservesVideoProfile` **立即变红** → 说明 video 的真实写法与该假设不符，**已回退**，video 保持原状（待独立确认）。**教训：修 A 时不要顺手改 B，除非有测试兜住。**
- **真机最终验证**：重新生成样本后 PowerPoint + WPS 均 `OPEN=ok` / `SAVE=ok`、`Audio 11 type=16`、`advanceTime=2.5` ✓（读取侧改动不影响产物字节，无回归）。
- **全绿**：`go test ./...` + `-tags=corpus ./...`、`go vet` 双口径、`gofmt -l`。
- **第 4 项（§15.3 第 3 条"播放记录"）仍未闭合**：产物能被两家客户端正确打开并识别为媒体形状（type=16）+ `advanceTime` 正确，但"音频确实出声"仍未留证据——需人工录屏，或 Windows 音频会话枚举（本机 `Add-Type` 被安全策略禁，难以纯脚本实现）。

## §15.3 第 3 条取证：CreateVideo 对照实验（commit `a6d8d2d`）
- **方法**：PowerPoint COM `Presentation.CreateVideo(file, UseTimingsAndNarrations=-1, DefaultSlideDuration=2, 720p/30fps/85)`，再用自写 `zz_mp4probe.go`（**纯 Go 解析 MP4 box 树**，不需 ffmpeg）判定音频轨。
- **结果（含对照组）**：go-pptx 产物 → mp4 时长 **2.508s**、**无音频轨**、形状 type=16；**PowerPoint 原生 `AddMediaObject2`** 插入音频 → mp4 时长 **2.508s**、**同样无音频轨**（box 结构逐项一致）、形状 type=16。
- **三条结论**：
  1. **`CreateVideo` 不渲染页内音频对象**——原生对照组同样无声 ⇒ **与本库无关**（`UseTimingsAndNarrations` 的 "Narrations" 仅指 PowerPoint「**录制旁白**」，不含「插入的音频对象」）。该路线**不能**作为"配音可播放"的证据。
  2. **但意外拿到两条可自动化的强证据**：① **计时被实际采用**——mp4 时长 **2.508s** 等于写入的 `advanceTime="2500"`，而非我传的 `DefaultSlideDuration=2`；② **形状与原生等价**——两边在 COM 视角同为 `type=16 (msoMedia)`，说明音频形状语义一致。
  3. **"出声"仍需人工**：放映（F5）时页内音频会播放，但 COM 采不到音频输出。
- **可复用工具**：`zz_mp4probe.go`（MP4 box 树 + `mvhd`/`mdhd` 时长解析 + `AUDIO_TRACK_PRESENT` 判定）、`zz_mkwav.go`（造对照用 WAV）。
- **方法学收获**：**对照实验是定性归属的关键**——若只看单边，"导出无声"很容易误判成"go-pptx 的音频结构不被 PowerPoint 认可"；拉入原生对照组后立刻定性为客户端既有行为。与 ADR-024 / Tier 2 的"必须先做对照"教训同源。
- **§15.3 第 3 条现状**：自动化可及部分**已到极限**（打开 ✓ / 识别 ✓ / 计时生效 ✓ / 与原生等价 ✓）；唯一剩余项是"音频出声"的人工录屏证据（格式 `.mp4`；`Win+Shift+S` 或 `Win+G`）。

## §15.3 第 3 条：人工录屏取证（commit `a432fe4`）
- **用户已录屏**：`.l3-output/s001-text.narrated.mp4`（643923 B）——**覆盖了先前的 CreateVideo 产物**（原 123159 B）。定位时先比对了 size/mtime 才确认是覆盖而非新增。
- **MP4 结构分析**（`zz_mp4probe.go`）：**2 条 trak** —— `vide`（400 帧 / 26.606s）+ **`soun`**（`smhd` + `mp4a`/`esds` 命中，**838 个 AAC 帧 / 423191 B / 平均 505 B / 最大 572 B**，等效 **≈126 kbps**），总时长 26.816s，`AUDIO_TRACK_PRESENT=true` ✓
- **自我修正（重要教训）**：最初的判据是"逐秒帧大小差异 → 定位有声段"，但实测**逐秒均值恒为 505±2 B** —— 这是 **CBR（恒定码率）AAC** 的特征：**静音帧同样被填充到固定大小**。故**帧大小/码率不能判定是否静音**，该判据作废。**教训：先验证判据本身成立，再拿它下结论。**
- **工具能力更新**：`zz_mp4probe.go` 现输出各 trak 的 `kind/samples/totalBytes/avgFrame/maxFrame` + 音频逐秒帧大小分布（对 CBR 无用，对 VBR 有意义，保留）。
- **剩余两项人工确认**（决定 §15.3 第 3 条能否正式闭合）：① 录屏音频源是**系统声音**还是**麦克风**（系统声音 = 直接证据，麦克风 = 间接）；② 人耳确认能否听到 2s 的 440 Hz 提示音。
- **本机无 ffmpeg/ffprobe/sox**，且 `Add-Type` 被安全策略禁 → **容器层分析已到极限**；解码 PCM 需外部工具或人工听。

## ✅ §15.3 第 3 条闭合 —— V2.6 五条发布硬门槛全部通过（commit `92eb63b`）
- **用户确认**：录屏音频源 = **麦克风**（非系统声音）；**人耳确认可听到声音**。
- **证据链成立**：`AddAudio 产物 → PowerPoint 放映 → 扬声器外放 → 麦克风捕获 → 录屏音轨（838 AAC 帧 / ≈126 kbps）→ 回放可闻`。麦克风路径经空气传播，反而**排除"仅软件渲染、实际无输出"**的可能（比系统声音直录更有说服力的一点）。
- **四项证据汇总**：① 结构——两家客户端 `OPEN=ok`/`SAVE=ok`、`type=16 (msoMedia)`，与 PowerPoint 原生插入的音频 COM 视角等价；② 计时——CreateVideo 导出时长 2.508s = `advanceTime="2500"`（非默认参数 2s），证明计时被实际执行；③ 对照——`CreateVideo` 不渲染页内音频对象（原生对照组同样无声），排除"音频结构不被认可"误判；④ 播放记录——人工录屏 + 人耳确认。
- **门槛状态**：`docs/release-readiness-2026-09-12.md` §2.2 由「4 闭合 / 1 部分闭合」改为「**5 闭合 / 0**」，并加勘误说明原"环境型缺口、非代码缺陷"的判断被实测推翻。
- **方法论沉淀（本轮最值钱的两条）**：
  1. **对照实验是定性归属的关键**——只看单边会把客户端既有行为误判成本库缺陷；
  2. **先验证判据本身成立，再拿它下结论**——CBR AAC 静音帧同样被填充到固定大小，我最初的"逐秒帧大小定位有声段"判据因此作废。**判据失效时要主动撤回，不要迁就结论。**

## ADR-026：视频形状同源缺陷修复（commit `76615d4`）
- **发现路径**：ADR-025 修完 audio 后顺带排查 `buildVideoPicFragment`（`video.go`），发现其产出**与 audio 原缺陷完全同构**——缺必需的 `p:nvPr`；视频引用写成 `p:videoFile`（**命名空间错**，正确是 DrawingML 的 `a:videoFile`）、缺必需的 `r:link`、位置错（应在 `p:nvPr` 内）。
- **实测复现**（与 audio 修复前逐项一致）：`s001-text.video.pptx`（SDK `AddVideo` 嵌入 mp4）→ PowerPoint **`0x80070570`**；WPS `OPEN=ok` 但只认 `Video 11` 为 **type=13**。
- **取证（关键一步）**：用 COM `AddMediaObject2` 让 **PowerPoint 原生插入同一 mp4**，读其实包 XML 得到权威形式：`<p:nvPr><a:videoFile r:link="rId2"/></p:nvPr>`（DrawingML + `r:link` + 位于 `nvPr` 内）；`blipFill` 的 `a:blip` 指向**封面图**（PowerPoint 另存了 `image1.png`）；另有 `p14:media` 扩展（本次未引入——audio 的最小实验已证非必需）。
- **修复**：写侧补 `p:nvPr` + 引用改为 `a:videoFile r:link` 移入 `p:nvPr`；读侧 `picMediaKind` **追加**正确命名空间探测并**保留**旧 `p:videoFile` 分支；`hasVideoFile` 复用 `picMediaKind`（消除重复实现，与 `lastProfileByMedia` 复用 `parseAudioProfile` 同一模式）。
- **上轮教训得到验证**：ADR-025 期间我曾"替换"video 命名空间 → 被既有测试当场拦下；本次改为"**追加**" → 全绿。**兼容探测要追加，不要替换。**
- **真机复验**：修复后 PowerPoint + WPS 均 `OPEN=ok`、`Video 11` 均 **type=16 (msoMedia)**（修复前 WPS 仅 type=13）。
- **守门**：`video_ooxml_compliance_test.go`——片段合规（断言 `a:videoFile r:link` 在 `p:nvPr` 内、不在 `blipFill` 内、不含 `p:videoFile`）+ 读侧双形式兼容（4 子用例：正确 video / 旧 video / 正确 audio / 普通图片）；**注入退化验证必红**。
- **一般化教训**：**这是一类缺陷，不是两个独立事故**——同一套错误写法被复制到 audio/video 两个函数，读侧也各维护一份重复探测。**修复一处后要主动排查同类复制点；真机验证必须覆盖每一个。**
